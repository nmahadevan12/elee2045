This is the transcription of the audio from the video, followed by an AI summary.

### **Audio Transcription**

**Speaker:** Alright everybody, good morning! It’s been a week, I know. Hopefully, we’ll have class this coming Monday—nobody knows, pending ice storms and whatnot. 

You all know that you have this assignment due. This is the basic one that I sort of did at the end of class—well, me and Gemini did at the end of class—where you could kind of click through and get the robot arm. You can also kind of drag these as well. There are some things that I don’t like about this at all, and that’s what I want you to go and say: "What’s wrong with this program? How could this be better?" 

There are several things I don’t like about it, ranging from visual things—like why is the 'X' overlapping that little box? That just bothers me—to the fact that it’s kind of slow; it’s not running really nicely. Why is that? Trying to analyze that and figure out what’s going on—all these things are, of course, possible. Some of it can get a little bit advanced, but they’re not necessarily that hard to understand.

Today, we’re actually going to dive back into some more basics: go through some more basic Python programming, go into some GitHub and whatnot. All I really wanted to point out to you all last time was that you can do this very easily with AI nowadays. Whether or not you understand it is up to you, right? And so, can you dive into the code? Do you understand the different structures that are going on? Do you understand the mathematics behind it? Programming is not engineering necessarily; the engineering of all this, the science of all this, is the underlying mathematics, and all that is exposed in the program—it’s inherent to the program what the mathematics are behind this, the physics behind this, and how it actually achieves its goal.

There are other things I don’t necessarily like, like what does it do when it reaches here? Why isn’t it trying to reach that? It just says "Target unreachable"—all right, it does that. Lots of little things in here that could be better, ranging from probably not using `matplotlib` to do our plotting or doing it a little differently. There’s a reason it’s taking so long to update. Then there are the visuals, the size of the figure—lots and lots of things to look at to try to improve. This is just a first go at it, and the code is probably kept pretty simple as a result.

Another thing we talked about last time was Git and GitHub. We talked about Conda, and we talked about basic Python installation and things like that. I’m going to kind of go through that again a little bit. I did summarize the poll over on Discord—remember to check in like that. I ended the poll over here, and it seems like, at least for those of you who filled out the poll, most of the time the issue is with—well, people say GitHub, but they probably mean Git. Remember, these are two different things—two very, very different things in practice. GitHub is *not* Git. GitHub is a service, basically, that hosts Git repositories (and really other things as well) in the cloud. That is what GitHub is; it is a host. It’s a place where essentially files are stored, because from the perspective of Git, GitHub is just another place where it can put stuff. It puts stuff on your computer, it puts stuff on another repository on some other computer you own, and it can put things anywhere that it can reach over the network. And the things it puts are commits.

So, we’re going to dive a little bit more into that difference today: Git vs. GitHub. We’ll talk a little bit about the theory behind Git as well today and go into some basic Python programming. Before I do that, though, let’s take a look at this program again because I just kind of ran it at the end there; we didn’t really discuss how it worked. This is usually necessary to go and edit it further. Even if you’re using AI—you’re probably using AI—it’s a good idea to look at the code so you can have a more productive discussion about what needs to change or why certain things are the way they are.

I’m going to go into VS Code now. I’ve already activated my environment—I did that before class. That was `conda activate ele2045`. That only works if you named your environment `ele2045`. If you want to list your environments, you can do `conda env list`, and you’ll see the environments that are on your computer. So these are the things that you can activate. If you have one activated, you can `conda deactivate`, and that’ll bring you back to your base environment. If you deactivate your base environment, you might get yourself into trouble because you might actually lose the ability to type `conda`, but hopefully, that’s not the case. I’m going to `conda activate ele2045` and go into VS Code. `code .` will open up VS Code. Remember, if you’re on a Mac, there’s a little bit of extra work there; ask AI what to do—it’ll basically tell you you have to go into VS Code and tell it to do `code .`.

It's already activating it for me, which is really nice. It’s using PowerShell. We don’t have to have VS Code use PowerShell; we can change that around if we want and have it use something like Git Bash. You can choose the terminal that activates. I think I can even add it here, so if I wanted to do a Git Bash terminal, I can inside of VS Code. But remember, one of the better things that you can do really is just separate these two things out. Even though it’s convenient to hit play, it’s not that inconvenient to go and write `python main.py` and run it this way. I like that personally; I know I’m running it from the folder it needs to be run from, I get to see my output, it saves—I don’t have to deal with sort of managing this one window that has the terminal and the code and everything in it at the same time. So I prefer to run things via the command line rather than trying to run them via VS Code, which is really running via the command line; it’s just running in that sort of integrated terminal.

I could also have hit play here, and it would do the same thing in the default shell. It’s actually doing some weird stuff; I don’t even know why it’s doing it that way. So that’s something to solve potentially. But what you want to make sure is happening is when you run it on the command line, you are getting the output that you expect.

I accidentally closed my Git Bash, so I’m going to have to go back to that folder. I’m going to write `git status` here to sort of see what’s changed in here. We’ve got an untracked file, `main.py`. We’ve also got this `.vscode`. This is also a really good time where you start saying, "Is this thing that’s untracked here something I actually want in my Git repository?" I mentioned `.gitignore` in the assignment. `.gitignore` is a very important part of Git. It is a file in your repository that says what should Git *not* pay attention to.

The thing that’s probably poorly understood there is that it explicitly ignores things that would otherwise happen. It doesn’t ignore stuff you don’t want it to ignore. So, if you’ve explicitly said, "I want you to add this change and then commit this change," it’s not going to ignore that if you explicitly told it to do something. It’s the stuff that’s implicit. So, if I were to write `git add .`, for example, which says "Git, add every change in this folder" (the dot standing for the current working folder), then it would add `.vscode`. It’s telling me explicitly: untracked files: `.vscode`. If I `git add` this, it will include it, right? And so I don’t want it to do that.

You can get yourself into trouble if you do `git add .`. I always type `git status`. Never don’t type `git status`. It’s a really bad thing to do because you can’t see what’s going to happen when you commit, and then you end up committing something you didn’t want to commit, and then you have to unwind the commits. That’s okay, it can happen, but it’s just kind of annoying to do, and there are sort of difficult things that happen when you end up pushing that, somebody else pulls it, and now you’ve got different repositories—it becomes kind of a mess.

But let me `git reset`. `git reset` will say, if I’ve added something, "go back." Now, that doesn’t work once you’ve committed it. Once you’ve committed it, now you have to actually unwind that commit. You can either amend a commit (that’s one option), or you can actually remove the commit if you want, if it hasn’t been pushed. So you can fully undo it. If it’s been pushed, again, now you have a problem because once it’s somewhere else, you don’t necessarily have the ability to edit that side—wherever it is, right? So pushing is not something you do every single time for that reason, especially when you’re working with other people. You kind of want to make sure that the commit history that you are going to put up on GitHub is something you actually want on GitHub. Now, for the most part, you’re working with yourself in this class, so it’s not actually that big a deal. But remember that you do not have administrator rights over your repository, and so if you happen to push something that you didn’t want to push for whatever reason, the only thing you can do is actually push the opposite change to the repository—you can’t overwrite the commit history that’s over on GitHub.

Alright, so how do we deal with this? I don’t want to add `.vscode`. Well, we need a `.gitignore`. I don’t have a `.gitignore` in here. Now, `ls` by itself would list the contents of the folder, but it doesn’t show you everything by default. So `ls -al` is what I usually type when I want to see everything. There are a few different ways to do that, but if you do `ls -al`, it shows you all of the files in the repository. Of course, we can also do this editing via VS Code, and VS Code by default will show you everything, so it shows you this `.vscode` that’s here. I’m going to refresh that. VS Code’s Git integration isn’t the best, in my opinion—it takes some time to update and things like that, so it’s a little bit messy, which is why I almost always do things via command line with Git.

Anyway, so we’ve got this `.vscode`. `.git`, by the way, which VS Code is hiding from you—it hides some things but not all things, it’s a little bit weird. But `.git`: what is `.git`? Does anybody know what this is—like, what is this folder? Where did it come from? It came from creating the repository. That *is* what makes this a repository. A Git repository is not some special folder on your computer or anything like that; it *is* a folder—a parent folder, the root folder, if you will—that has a `.git` folder. So if I were to take this `.git` folder and put it in some other folder, *that* becomes my Git repository. It is not the files that are even in the folder, okay? That’s called your working directory; that is not necessarily a part of your Git repository, just what happens to be there at that particular time.

What Git "knows" about, effectively, when you run commands, is what’s in that folder. So that is where the history is stored. When you `git add` something, when you `git commit` something, when you change a remote—those things are stored in that folder. And so if you actually were to go into that folder, `.git`, you can see what’s actually in here. For example, we can look at what’s in `HEAD`. `HEAD` is a reference to where the next commit is going to be tacked onto, which is currently a reference to a branch. And a branch—if you actually go into the branches, which is somewhere in here, it’s probably under `refs/heads`—and we `cat main`, we can see it is actually a reference to a commit. It’s this weird identifier. And if we `git log` (and I usually do `--oneline`), we can see the `31c2423`, which `main` points to: that particular thing.

So again, the `.git` folder: do we edit this? Never. We never edit the `.git` folder, we never touch this. But it’s kind of interesting to look at it, and it really tells you a lot about how Git works internally with these names—things like `main` and `HEAD`, which will crop up from time to time when you’re using Git. Like if I wanted to go to the previous commit, I would go to `HEAD~1`, and that takes me to the previous commit. So some of this terminology is directly from sort of the structure of this Git repository.

This is a very important part of Git: these identifiers. This is what really exists, right? These identifiers are essentially a hash, meaning a unique way of capturing the bitwise data that is in this commit. You can actually create a hash from almost any binary structure of any size, and you’ll get this unique thing that’s unique in all the world and has a very, very improbable chance of ever being generated from another binary sequence. It’s almost astronomically small, it’s never going to happen. And so these are unique; they’re generated from the data that that commit represents and is this unique identifier that you can use for various things.

And so, what actually *is* a Git repository? If you really dial down into it, all it is is this effective tree—and that’s why where this terminology like "branches" comes from. But it’s a tree, and that tree is defined by a series of commits. And so each of those commits—you have a commit that’s sort of your base initial commit. Nothing in there, sort of—usually that’s why the first commit in the repository is called "First commit." Right? So there’s this base commit, and it has this identifier. And then when you commit again, that new commit has a pointer back to the previous commit. And so that starts creating a chain. So as you add commits, you get a chain of commits, each one with their ID and that has an identifier of the commit for it.

Now, the really interesting part and why it’s called a tree typically is that you can create these branches. But the branches aren’t really anything special as we’ve seen—the branches are just a pointer to a commit. But the branches happen when you do a merge, and so there’s a special commit known as a merge commit that has a pointer essentially to two places. And so when you merge, you get a branch, and you have the ability for a reference essentially to two places. And so when you have branches, all they are are pointers to commits. That’s it. They are a label, essentially, that points to a specific commit—that’s all a branch is. Which is a weird thing because you can have branches that, if you were to visualize it, you have two branches and they’re both on the same visual branch because all a branch is is a pointer to a commit—a label for it. That’s really all it is.

Git is—all it is, a Git repository—is a tree of commits. That’s all a Git repository is. What is a commit? Is actually a very interesting question. What does it mean to commit something? Is a commit a file? No. Is it a set of files? No. A commit is a set of changes. A commit is a set of instructions of how the repository is to be changed. So a commit is not a set of files. A repository is not a set of files. A repository is effectively this tree, this directed tree of changes to be made to a repository.

And that is how Git actually allows for very elaborate mergers between team members and things like that, because all Git will do when you tell it to go to—change your working directory effectively to check out—you’re telling it to follow this set of changes. And so when you merge, as long as the changes are compatible, it’s able to put it all together. And so it’s not files or folders or anything like that; it’s a set of changes that when you go through those changes, you will result in this current working directory. The current working directory is the result of applying your Git commits and then any changes that have been made subsequently.

Git does not actively track anything. It does not actively do anything. It is not Dropbox, it is not Google Drive. It does not sit there and try to synchronize to the server automatically. Nothing happens in Git unless you run a command. You have to run a command. So the only thing that will ever change your repository is `git add` or `git commit`—that actually makes changes to the `.git` folder—and then `git push` actually sends those changes up.

We actually see several things when we look at the `git log`. The `git log` really tells you everything you need to know. You’ve got `HEAD -> main`, and Git Bash, which is what I’m using—it has Git in the name—it colorizes things to make it a little easier for you to look at. And so it tells me that locally, this thing in blue and really where `HEAD` is, points to my local `main` branch. So the green thing is the local over here, and you may or may not see this if you’re using Mac—you’re not going to see these colorings—but you should be able to still see the same text here. And then we also have this `origin/main`, `origin/HEAD`. What the heck is `origin`? Where did that come from? I didn’t create that. So `main` definitely got created; this `origin` thing, where did that come from? Well, `origin` is a remote. So `git remote -v` will show you your origins. And you can have 50 origins. These are other places that you have in your Git repository specified that you want to track what’s sort of going on over there, and you want to be able to push and pull from.

And so when I do `git status`—let me go `cd ../../../`. Alright, that should be—`git status`. We can see my branch is up to date with `origin/main`. So it knows that there’s an upstream branch for this particular local one—there’s a terminology in Git called being "upstream"—and so a particular branch (and this is again all in the `.git` folder) it specified essentially that I want this branch to track this remote branch. That’s all.

And so when we initially cloned our repository from GitHub, all of this was set up already in the `.git` folder. So GitHub—again being the service—created a Git repository, and then `git clone`—which is how we got the repository—cloned that repository, essentially copied the `.git` folder and changed a few things so the names are consistent on the local side. But `git clone` is an operation the Git command-line tool can perform. It cloned that repository and automatically set up these remotes. If we were to flip that, it’s not that hard: we could have initialized a repository locally and then pushed that repository up to GitHub. And it actually gave us some instructions if we wanted to do that already when we initially created the repository via the GitHub Classroom link.

So again, Git—there’s a lot of stuff in there, but suffice it to say, Git is this tree of changes. That is all Git is. And if you can appreciate that, then Git will make your life a lot easier, rather than trying to think of it as anything else. Just think about it as this tree of changes. You can unwind those changes, you can navigate around that commit history actually very flexibly. You can check out a particular commit, you can make changes. When you get yourself into weird states like a "detached HEAD" state, you can go back to where you were pretty effectively.

The only two commands that you really have to worry about with Git—the only two things you really have to worry about where people can get themselves into a little bit of trouble—one of them is `git checkout`. So `git checkout` can be dangerous. You can check out a specific file, and when you check out a specific file, it will actually overwrite your current file, and it won’t warn you. That’s one thing. So be careful when you’re checking out specific files. That explicitly says, "Change my working copy to what’s in the Git repository." Change that file to a particular commit—meaning the result of applying all those changes. So that’s one way you can get yourself into trouble that I’ve run into problems with, because it doesn’t—even if you have changes, it’s like, "Cool, we’re just going to replace it," because it thinks that’s what you want to do; you’ve explicitly told it.

The other thing is that you can do a `git reset`, but do a `git reset --hard`. So `git reset` by itself will not overwrite anything. `git reset` is fine—you saw me run that earlier. `git reset` by itself will just say, "Hey, anything you’ve added to the repository currently, any changes, I just don’t want to add those anymore, I don’t want to stage those anymore, I want to go back." So it doesn’t make any changes. When you do `git reset --hard`, it actually forces the changes to go away. It will kill your current changes to your working directory. So `git reset --hard` is something that you have to be careful of. I run it all the time because a lot of times I just didn’t like what I did, I just want to go back to a clean repository—just stop. And so `git reset --hard` can get yourself into a little bit of trouble; just realize that will change your working folder. `git reset` is not bad, but `git reset --hard` changes the working folder.

`git checkout` also potentially bad—so if I did a `git checkout main.py`, it might overwrite it. Now in this case, there is no `main.py` that’s known in my Git history; I never committed anything from this repository—at least I don’t think I ever committed `main.py` to this repository because it’s untracked. So this actually wouldn’t do anything, `git checkout main.py`, or at least it shouldn’t. It would only change that if I actually had a file called that somewhere in my change history.

So, let’s add `main.py`. There are two ways I can avoid adding `.vscode`. One way I can avoid adding `.vscode` is by literally not adding it. So I can write `git add main.py` and completely ignore `.vscode`. Now, why don’t I want `.vscode`? I probably didn’t talk about that. What is `.vscode`? Well, `.vscode` was created by VS Code. What’s in it? We can go in there and see what’s in it. `.vscode/settings.json`: default environment manager, default package manager, Python projects... You have to ask yourself, "Is this something I want other people to have? Is this something that is necessary for other people to have when they clone my repository?" It’s not always obvious. How do we figure that out? Well, VS Code’s not being very helpful to us, right? It just puts that there.

So how do we figure that out? Well, we can Google it. And actually, it’s a really good idea to do that, or we can ask AI. So I’ve got Gemini over here, right? And we can say, "Should I commit the `.vscode` folder?" I made the mistake of linking Gemini to my entire life. I’m going to unwind it because it’s actually driving me crazy. It’s like, "In your email four weeks ago, you said this." It’s like, "Okay, bad idea." Sometimes giving people too much access changes their interaction with you.

Generally, the answer is no, but yes for team configurations. Okay, so it’s a "maybe." We’re not working in teams; I’m going to ignore it for right now, so I’m going to add it to the `.gitignore`. Another thing you can do is say `gitignore vscode`. So if you Google that, "vscode gitignore," a lot of times GitHub will have it. And so it explicitly says, "Okay, this is a good `.gitignore`, a good set of things to put into `.gitignore` for a particular development environment." And so there’s lots of development environments out there—VS Code just being one of them, right? MATLAB, I use Unity 3D—there’s a `.gitignore` probably for that. And so `.gitignore`s, of which there can be many, by the way—every single folder in your repository can have a `.gitignore` in there that overrides the `.gitignore` from the parent folders. `.gitignore` is very, very useful because it will automatically ignore specific files when I try to add folders. `.gitignore` applies when you try to add folders. It will also apply in `git status` to tell you what is untracked. And so it’s sort of like, when you don’t specify something, what are the defaults?

And so I’m going to grab this—I’m going to grab the contents of it. We can just also copy the raw file from here, and I’m going to edit my `.gitignore`. Again, we can create this in a variety of different ways. I almost always use the command-line tool `nano` for this. So `nano .gitignore`. The reason I don’t use VS Code for stuff like this and I try not to use things like Explorer or Finder or whatever for this is file extensions are kind of a nightmare in most graphical user interfaces. They tend to try to help you too much. And `.gitignore` is the actual name of the file. It’s not `.gitignore.txt` or whatever, `.md` or anything like that. It’s not really a file extension. That dot has a history. That dot is a convention in Unix systems that this thing is supposed to be hidden when you are visualizing it to a "dumb user" so they don’t delete it accidentally. So the dot is intentional to hide things or hide it from—because it’s not intended to be edited by someone who doesn’t know what they’re doing. But these graphical user interfaces, right, that are intended for people who don’t know what they’re doing, they tend to—like when you create a new file—automatically add a `.txt` or assume you want that or something like that. And this is a raw text file; when you use `nano .gitignore`, it just assumes you know what you’re doing. You’re using a command-line tool, you probably know what you’re doing.

Alright, so it’s open now. I’m going to paste this in here. So we’ve got this VS Code. Let’s look at the—I mean, `.gitignore` is actually a very, very simple thing once you extend your understanding to its pattern matching. So you can directly say `ignore .vscode`, but this is actually saying `ignore everything in .vscode` (so that’s `.vscode/*`). If I just put `.vscode/`, it would ignore everything in there by default. So `/*` is sort of redundant. But then it’s saying "Do *not* ignore this, do *not* ignore this, do *not* ignore this..." Right? So let’s start with "ignore everything" but except for this, this, this, and this. So the exclamation point in front says "don’t ignore that specific file." And so this is saying these are the things that most programmers would agree you shouldn’t ignore. Other people who clone your repository might need these to ensure that if they use VS Code, they get the same general setup. That’s essentially what it’s saying.

So I’m going to `Ctrl+X`, say yes. `git status`. We still have a new file, `main.py`, because I added `main.py`. Untracked file: `.gitignore`. It specifically is saying we’re going to commit that. So I didn’t know the answer—should I commit that `.vscode`? And the answer is yes, I should commit that VS Code. Now, what’s probably the case is that that file’s not going to change every single time I open up VS Code, because that would be really annoying. Those are the things that you don’t want to commit. You don’t want to always have changes to the repository just because you opened up a development environment and maybe resized a window. And those things can be in these sort of configuration files. That’s why `.gitignore` exists, so you’re not constantly committing stuff that’s an innocuous change. So don’t commit things that you don’t need to commit. But it’s not always obvious, and so I always consult the internet before I just `.gitignore` something that I don’t know if somebody else needs it.

Now, the foolproof method to know whether or not something is needed is check out your whole repository—or really clone your whole repository—to another folder on your computer, which you’re free to do. So you can clone this 500 times if you want to. And each of those clones can be in different branches, different states, whatever you want, because they’re just clones and they’re all essentially the same. And those clones, if you clone them from GitHub—you can also clone them from your local repository, but I recommend you clone them from GitHub—those will all be essentially pointed to the same place.

Alright, so now we can `git add .`. And the reason `git add .` is good now is because we have that `.gitignore`. So `git add .`, `git status`. Okay, it’s going to add these things; it’s going to add the `settings.json`. Another thing I should mention about Git: it’s a set of changes, or a tree of changes. But it’s a tree of changes to files. There are no folders in GitHub—and Git. I use them synonymously, too. So there are no folders in Git. You do not change folders; folders do not exist. In fact, empty folders cannot exist. You need to have a file. So a lot of times there will actually be like placeholder files in folders that need to exist because the program depends on them existing. Which sometimes can cause problems if that program doesn’t explicitly create the folder. So Git is a change to files—changes to files, not changes to folders.

Now we’ve got these things added, we can do a commit. `git commit -m` and we write a commit message. If you’re doing this via the command line, "add..." and lots of times people use like active verbs for this, like "This change... if I..." So the way you could think about a Git commit is you should answer the question of "What happens if I commit this?" And so "Adds .gitignore and a first run at robot program." AI really should be writing our commit messages, if I’m being honest, because most people like myself are not capable of writing effective commit messages. So AI is a good thing. I wanted to show you sort of the command-line way. We could of course also go into VS Code here, which now shows that there are no changes, by the way, because it did update, and let it write our own commit messages. So if there’s one thing that probably should go away in the world, it’s human-written commit messages.

But that actually made a commit. And so if I run `git status`, we see my branch is ahead of `origin/main` by one commit. Nothing to commit, working tree clean. If we do `git log --oneline`, we see that we have a change here. Now my `HEAD` pointer—where I would tack on changes—points to `main` still. Nothing changed there. `main` points to this particular commit. In my repository, the previous commit is this one, and that’s where `origin/main` points to. So `origin/main` and `origin/HEAD`, which are two pointers I’m also aware of, which are GitHub’s—really that remote that happens to be a link to that GitHub repository—that’s at this commit. So I know that’s where they are. At least I know that’s where they were. I don’t know that’s where they are right now. Nothing happens in Git except for if I ask it to. So if I `git fetch`, `git fetch` will go to my remotes and update their history. It’ll say, "Do you have any commits for me that are known there? Are there new branches? Are there... where are you at right now? Are your pointers changed around?" And then it’ll be able to compare, and then if you run `git status`, you’ll actually be able to see exactly what’s going on on the remote.

Nothing from my computer is yet on GitHub yet. `git push` is what makes that happen. So when you `git push`, it says, "Hey, current branch..." and unless you do `--all`, it’s not going to push all of them. But "Hey, current branch with here I’m on the `main` branch, anything that is not on the remote, push up there." And you have to pretty much be ahead of the remote to do this. And so if you’re not ahead of the remote and the remote’s ahead of you, typically you have to do a pull first. Pull being actually a combination of two commands: `git fetch` (what’s there) and `git merge`. And so it’ll actually create that merge commit between your local branch and the remote branch that have the same shared history but a diverged path. We have two different sets of commits that are pointed at the same commit. And so what actually has to happen is we have to merge those two, have a merge commit, and then our changes now will essentially stack on to each other and they will have a perfect shared history.

So `git push` right now will work because I’m just ahead, and so whenever you’re ahead, you can just commit. And so if you’re ahead and behind, it’s going to have to create a merge. And if you are behind, it’s going to force you to pull before you push. If you had admin rights, you could force a push, which would overwrite whatever anybody else did—this would not be the best, usually it’s never a good strategy to overwrite unless you really, really feel like you can’t have some history in that repository. I’ve had to do this on many occasions, actually. And the reason is that I accidentally committed something that’s like proprietary or has a password in it. You obviously can’t leave that in the history because then anybody who clones your repository would get that thing they’re not supposed to have. And so you have to take it out. And so that is the one reason—so please don’t commit anything proprietary to your repository because then I have to change it because you don’t have the right to do that.

So I `git push`, and that will actually push it up to GitHub. I very much trust what happens here. A lot of people say, "Oh, I pushed it, it didn’t actually go up on GitHub." If it said it did, then it did. Now, if you don’t pay attention to this message here, it may not have happened. Again, this is why I tend not to use VS Code—because I don’t get these messages in VS Code. This tells me exactly what happened. VS Code doesn’t tell me what happened. Now, if you want to look at things in VS Code, like see what actually happened—go through your different commit history, look at your graphs, see changes—wonderful tool for that. Command line: terrible tool for that. So if you want to look at your repository: graphical user interface. If you want to change your repository: command line is what I recommend. And so we can see what changed, we can see individual changes—this is a whole file that we added—we can see that `settings.json`, the `.gitignore` that’s in there, we can see all of that via looking in VS Code.

Alright, so hopefully that helps with GitHub and Git. Remember, GitHub... I haven’t even gone on GitHub yet. GitHub’s not actually that relevant. Why does GitHub matter? Well, GitHub matters when you want to share a repository with other people. Right? I guess I did kind of go on there... so `github.com/ele2045/sp26/assignment-1-kjjohnson`, and we should see on here that all the stuff is up there. We’ve got six commits in there, we can see those changes on here. These identifiers are going to be the same, so those identifiers are what Git is using to figure out, "Okay, what commits do you have? What commits do I have?" It’s not actually looking internally in these; it’s just looking at those identifiers. We can see our different branches, which we can change up here. Again, being pointers—convenient pointers to commits in our repository history. So creating a branch is just creating a pointer; that’s all it is. There’s no folder structure effectively in the commit tree.

But you probably don’t need to create branches at this point. Branches have a lot of use cases in large engineering teams. Individually, they can be "I’m working on a feature that I don’t necessarily want to... it’s not sort of my main feature set." I’ll give you an example from my research. A lot of times we will be creating a system that people use, and we want to run a research study—we want to add all this stuff for the research study that’s all this logging and special configurations and things like that—but I don’t want to actually add that to the main product that people are using. And so I’ll create a branch and then build off of that branch, and that just sort of becomes a dead end for that particular research project, but all the changes are logged and things like that, we can go back to that particular version at that point. That’s where we use branches for. We can also add branches if two people are working on a repository and we’re going to be messing with the same files and things like that. Now, that creates a lot of problems because now you’re almost certainly going to be changing the same files, and when you go to merge the branches, you’re going to have to go through literally in many cases line-by-line: which one of these do I want? How do those merge together? And Git is not smart enough—maybe with AI someday it will be—but it doesn’t necessarily know what’s in your head to merge two changes to files where you know which line goes before the other one, and it’s going to result in a working program that does what we want it to do. So I do think AI plays a role here, but it’s a very painful process to actually do Git mergers. So try to avoid them if you can. So if when you’re working on a team, I tend to first go to coordination over automation. Coordination over automation in most cases when you have that opportunity. So if you can say like, "Hey, I’m going to be editing that file, maybe you should stay away from that file," rather than two people doing the exact same work.

And again, the right way to test my repository is not to run this and say everything’s good; the way to test this is to back out of it. Now I’m just in this folder, it’s not a Git repository. Oh, another really big thing that people struggle with with Git: they put repositories inside of repositories, and they didn’t realize that they did this. So a repository inside of a repository inside of a repository means that you have a folder with `.git` and some child folder or some parent folder also has a `.git` folder. That creates—you can do that, and you can commit that, and that creates in Git parlance what’s known as a submodule. So it’s a Git repository that depends on another Git repository. If you push that and commit that, it’s kind of hard to unwind; it’s really annoying, so try to avoid that at all costs. `git status` will always basically tell you what’s going on.

People get themselves into trouble because they do `cd /` and they do `git init`. Now you have a `.git` folder inside your root, which is now the parent Git repository of like every repository on your computer. This is not a good idea. And so I could do that, I could do `git init` and it says "permission denied" because it doesn’t have write access to this, but I could force it and then I’d have it. But like, `git init` right in here—this will allow. Now it initialized this. Now every repository that’s a child now thinks this is its parent. It’s imprinted on these children Git repositories. But easy to get rid of it: `rm -rf .git` in this folder will delete it. So that’s "remove recursive force"—this is how you can get rid of folders. You need to be very careful when you actually do this. So `.git`, I’m going to be very specific: I want that folder. And it’s gone now, and `git status` will tell me I am not in a Git repository. If you type `git status` in your home folder and something other than this shows up, that’s bad. That means you have a Git repository in your home folder—you probably don’t want your Git repository there.

So let’s go over into my actual repository, which is `/d/gitrepo/ele2045/sp26/assignment-1`. So there’s where we’re at. We’ve got our `main.py`. `git status` should all work now. Everything is good. We’re up to date with `origin/main`. We’re ready to go.

Alright, does anyone have any questions in here? No? Good. Very nice. 

**Student:** Did you use Conda to get the `main` on the user statement? 

**Speaker:** No, that is actually the branch that I’m at. So this is Git Bash. You’re not using a Mac, so you should actually see this if you’re using Git Bash. So Git Bash, which you can Google, you can download Git Bash if you don’t have it, but it should show `main` on here. If you do not see `main` here, it probably means you are not in a repository folder. So if I’m just in like `cd ~`, that’s going to go away—I don’t see `main` there because there’s no Git repository there. If it’s not showing, there are a few different ways that you can edit the shell prompt that you see. Where this actually is is in this tilde folder, so if I `nano .bash_profile`, I believe that’s where it is... No, well it’s running `conda eval` here... `shell.bash`. This is... if I were to find this inside of this folder, this `shell.bash` probably shows me like how it creates that prompt. It’s a fairly elaborate process to create the prompt every time you change the working folder, so there’s a lot of stuff happening when Conda is actually—not Conda, when—that’s actually Conda. It’s not Git Bash, this must be somewhere else. Let’s see... `nano .bashrc`? It would be somewhere in here. I would have expected `.bash_profile`. It might actually be part of the Git Bash program now that I think about it, because this is an actual program, Git Bash is a terminal, and so it’s probably doing that inside of here. So you should see it if you’re in a Git folder. So make sure you’re in a Git folder; that must be how it’s actually creating this prompt in here. It's not a configuration. You can also change your prompts on most shells—so like Bash itself has a prompt that you can change—so what it shows. So this dollar sign here is a Bash setting. I think it’s the default, actually, but you can change it to show other things like working directories and things like that. So Git Bash is a little bit different—it’s what shows you here on this line.

Alright, so there was a thing that I was going to talk about today, but I spent a lot of time talking about Git, which I’m likely to do in many cases. So I had a whole lecture here I was planning on, which is fine, but it has a lot of this stuff in here. So there are some Git in here, right, so if you were wondering about those commands, it’s over here.

Alright, so in the next 15 minutes or so that we have in class, we’re going to talk through just generally speaking programming in a nutshell. What is a program? At a high level here, let’s go into reading view over there because the output is wrong over there. Sorry, everything is difficult. 

Just a quick assumption this assignment’s not so bad because I haven’t gotten a lot of feedback on it, but please reach out if you’re having any trouble with the assignment, need any additional help. Remember you can talk to me after class for like until noon or so. But anyway, high-level overview on programming, okay? What does a computer program actually do? A computer program, all it does—this is very, very low-level theory of computing, okay? Computers are not magic—in fact, they’re incredibly simple devices. If you were to boil down a computer to what it is at its core, the only thing it actually does is these trivial logic functions. It takes essentially on-off switches. So we have that bank of on-off switches, alright? So it’s got this bank of stuff—these are transistors, you’re all electrical engineers, you’re going to learn a lot more than you ever wanted to know about transistors probably. But it takes these banks of electrical devices that are basically on-off switches, alright? And it implements a lot of functions with those basic on-off switches.

And it also has, essentially, builds these gates from it. And the simple gates are the ones that are like AND gates. AND gates basically say that hey, if this is on and this is off, the output is off. And you can build these from the electrical interaction of these transistors. So how they’re actually hooked up, meaning that if there’s a voltage applied here, a high voltage, and there’s a low voltage, the output of that thing is going to be a low voltage if it’s an AND gate. And again, this is sort of an elaborate arrangement. Once you have an AND gate, and typically you also have these NOT gates or inversion gates which can take a high voltage and turn it into a low voltage, you can also create OR gates which are high and high become high, or high and low become high, just low and low become low. Once you have these basic building blocks, which is what the computer is built out of—once you have these logic building blocks, you can build these structures logically known as truth tables, which are essentially lookup tables, meaning that "Hey, this more elaborate thing that’s not just simple ANDs and ORs, I want to behave in this way." And then you can build from there; you can build addition. 

And once you have addition, you can do almost anything mathematically. If you actually look down at most math procedures, they’re almost all addition. Multiplication: addition. Subtraction: addition, turns out. So what is subtraction really? It actually *is* addition if you work it out. In fact, computers use a little trick that another way to "add" is to—because memory is finite—another way to add is to keep going around until you go to the other side. So you have four bits, right, so that only gives you 16 possible values, and so instead of going back by one, you can go ahead by 15. And so computers actually use addition to do subtraction. Once you have subtraction, you get division. And addition, subtraction, multiplication, division... well, we don’t actually know how to do any of that if we look at the algorithms used to do any like long multiplication or long division and things like that—it’s all those operations, right? And so like these really elaborate things are built off of much simpler things: addition and subtraction. Functions like square root and stuff like that—again, all processes that have been built that are all done with addition and subtraction.

So computers are super, super simple. The actual computation part—the thing that does the math. If you add to it two additional things, one of which we’ve already talked about, you get computers. You have to have storage. So storage we already have—we have these ones and zeros, we have these switches that we can set to these variables or these values. And that’s a little bit of an upgrade—so you go from a transistor, which is just this simple device, to a storage thing, and typically that’s done with what’s known as flip-flops. You’ll learn about flip-flops. And so again, low-level electrical engineering type thing to build these storage structures that can hold values, right? And then from there, we need one additional thing beyond storage—and storage, by the way, is yeah, you have flip-flops, that’s what the CPU typically uses, but if you take it out to the limit, you have like tape storage and stuff like that, you have lots of different ways to store ones and zeros and read ones and zeros. But inside the computer, they’re typically flip-flops. 

And then the last thing you need is some ability to make decisions. Some ability to say, "If I have this, then I do this, and if I have this other thing, I do this other thing." That’s it. Storage, decision-making, and adding. That’s all a computer is. They’re not magic devices; they’re just really, really, really fast. And so nowadays, hyper-parallel, and so they can do a lot of stuff really, really fast, in parallel. And from there, you can implement almost anything—anything we know how to do, anything we can describe that we could write down on paper, we can make a computer do. That’s oftentimes the tricky part: how do we actually write down how to do something? Because a lot of what we do is locked up in our head, right? Like how do I reach out and grab this cup and pick it up and take a drink? This is actually very hard to describe as ones and zeros and math operations essentially, right? And we’re still struggling with that—we don’t have robots right now that can walk into this room and grab this cup and pick it up and not spill it and not knock into everything and deal with stuff that’s on the ground and do all this stuff. We just don’t know how to describe that stuff yet. It’s getting a little easier, right? So the things that we used to lack the ability to describe, we can now describe in human language, and we figured out this map between human language—which is Large Language Models—and numbers. That’s what a Large Language Model is, by the way: it’s a map, effectively, between human language and numbers, so that you can do computation on human language. That is, if you really dive into the details of it, that is what the magic is—is that map to numbers. So anything you say can be mapped to a very, very ultra-impossible-to-fathom high-dimensional space. You just cannot imagine how much empty space there is in there, where every concept that we can explore in human language we can map to numbers, and then those numbers now have meaning—when things are close to each other, they’re meaningfully close. That’s what Large Language Models do. That’s about my level of understanding, at least.

And so computers: very, very simple devices. Map input to output. That’s what they do, and they do that typically using these logic functions and make decisions. But again, storage and decisions are key tools of the program’s operation and key tools of the program’s design. So all programs pretty much are going to look like this flowchart, okay? They have some start, and they go across through a process of making a decision about what to do; they typically are going to loop around operating on something, producing some output; they use that input in making the decision, and they proceed from there. Every single program written on the planet basically looks like this until at some point the program ends. You can take any program you want and map it into that. Now, where things get complicated, of course, is that this procedure can have this entire loop inside of it, right? And then the other one can have another loop, and the other one can have another loop. And by the way, you can have a split in here where now you’ve got three things operating at the same time. And so that’s where all the complexity arises from.

I already did this, so I’m going to skip this over. And I was going to start with a simple problem—we’ll get to that. Hopefully, on Monday, we’ll dive into this a little bit more. I’m also going to introduce as soon as possible the microcontrollers for the class. And so hopefully Monday, probably Wednesday if the weather is the way it’s expected to be, we will have—you’ll have the M5StickC Plus's in your hands. We’ll start doing some C++ programming and Python programming at the same time and linking those two things together. So the class is largely going to be about going forward. So make sure you get your assignments submitted, make sure you’ve cloned them from GitHub, and you check and make sure that everything is the way it should be, okay? 

**Student:** By "submit," you mean "commit"? 

**Speaker:** Yes, submission is committing. Submission is committing—well, pushing. Pushing. What is on GitHub is your submission, okay?

**Student:** But I have committed it, but can you just check to make sure my submission is like, looks right or something? Because I know my program runs.

**Speaker:** Yeah... 

---

### **AI Summary**

#### **Overview**
In this lecture, the instructor covers the logistical details of an upcoming assignment, clarifies common misconceptions about Git and GitHub, and discusses the fundamental logic behind computer programming.

#### **Key Topics**

**1. Assignment Feedback: Robot Arm Simulation**
*   Students are working on a robot arm program (developed with AI assistance).
*   **Criticisms to Address:** UI issues (overlapping elements), performance lag (likely due to inefficient use of `matplotlib`), and logic errors (handling "Target unreachable" states).
*   **The Goal:** Students should not just generate code but understand the underlying mathematics and physics to improve and refine the simulation.

**2. Development Environment Setup**
*   **Conda:** The instructor demonstrates listing environments (`conda env list`), activating/deactivating them, and launching VS Code (`code .`).
*   **Terminal Preference:** The instructor prefers running Python scripts directly from the command line/terminal rather than using the integrated VS Code "Play" button for better control and visibility.

**3. Git vs. GitHub**
*   **Distinction:** GitHub is a cloud-hosting service; Git is the version control tool that tracks changes.
*   **Internals:**
    *   The `.git` folder is what defines a repository; it stores the entire history and branch pointers.
    *   Commits are not sets of files, but **sets of changes** (instructions on how to modify the directory).
    *   **Branches:** A branch is simply a label pointing to a specific commit.
    *   **Hashes:** Commits are identified by unique bitwise identifiers (hashes).
*   **Commands:**
    *   `git status`: Vital for checking staged and untracked files.
    *   `git add`: Use with a `.gitignore` to avoid committing unnecessary files (like `.vscode/`).
    *   `git reset`: Unstaging files.
    *   `git commit -m`: Creating history with descriptive messages.
    *   `git log --oneline`: Visualizing the commit history.
    *   `git push/pull`: Synchronizing local work with GitHub.

**4. The Theory of Programming**
*   **Simplicity of Hardware:** Computers aren't magic; they are collections of on/off switches (transistors) forming logic gates (AND, OR, NOT).
*   **Building Complexity:** Logic gates lead to arithmetic (addition is the basis of all math in computers), storage (flip-flops), and decision-making (flow control).
*   **AI Context:** Large Language Models (LLMs) are essentially complex maps between human language and numerical space, allowing computers to perform "math" on concepts.

#### **Action Items & Looking Ahead**
*   **Assignment Submission:** Pushing the final commits to GitHub constitutes "submitting" the assignment.
*   **Next Steps:** Introduction of **M5StickC Plus** microcontrollers.
*   **Curriculum Shift:** Moving from pure Python to **C++** and cross-linking the two languages in upcoming sessions.