This audio file is a classroom lecture focused on the M5Stick-C-Plus, an ESP32-based embedded system. The instructor discusses the hardware specifications, how to set up the Arduino development environment, and the fundamental differences between C++ and Python programming.

### **Lecture Summary**

**1. Administrative Announcements**
*   **Quizzes:** The instructor outlined the format for upcoming quizzes, which are closed-book and handwritten. They typically consist of true/false or multiple-choice questions based on lecture summaries and assignments.
*   **Study Tips:** Students are encouraged to use lecture summaries and even AI tools to generate practice questions to prepare for the quiz on Python and Git concepts.

**2. Hardware Deep Dive: M5Stick-C-Plus**
*   **Processor:** The device uses an ESP32-PICO-V3, a 32-bit dual-core processor. The instructor compared it to a miniaturized PC with its own display, memory, and I/O.
*   **Features:** Key hardware components include GPIO pins, pull-up resistors, capacitive sensing, a built-in battery, and radio capabilities for Wi-Fi and Bluetooth. It also contains an accelerometer and gyroscope.
*   **Pin Mapping:** Understanding pin numbers via schematics is crucial for interacting with specific hardware features like buttons and sensors.

**3. Development Environment: Arduino IDE**
*   **Toolchain:** To program the device, students must install the M5Stack board library in the Arduino IDE. This provides the necessary compiler and uploader for the ESP32 architecture.
*   **Serial Communication:** The device communicates with the PC via a virtual serial port (UART). Students must match the "Baud Rate" (communication speed, typically 115200) in the Serial Monitor to avoid garbled data.

**4. Programming Concepts**
*   **Arduino Structure:** Programs consist of `setup()` (runs once) and `loop()` (runs repeatedly), similar to a Python `while True` loop.
*   **Compiled vs. Interpreted:** The lecture highlighted that C++ is a compiled language (turned into machine code), making it much faster and more memory-efficient than an interpreted language like Python.
*   **Static Typing:** Unlike Python, C++ requires explicit type declarations (e.g., `int`, `float`, `char*`). This allows the compiler to manage limited hardware memory effectively.
*   **Real-Time OS (RTOS):** The ESP32 runs FreeRTOS, which provides "real-time guarantees"—ensuring tasks execute at exact intervals, which is essential for hardware stability.

**5. Looking Ahead**
*   The next session will focus on using Visual Studio Code and "vibe coding" (using AI agents to assist in writing Arduino code).

***

### **Audio Transcription**

**00:00 - 05:00**
Alright, very happy to see those of you who decided it was warm enough to come to class. For those of you who will watch this on video later, remember class is not optional. If there's—I know there's a couple people already talked to me—but you should have one of these, the yellow one or the red one. Either one is fine, but if you don't, please reach out to me. This is what we're going to be using for the most part as a piece of the puzzle for the rest of the class so that we can program on an embedded system and also sort of struggle with integration.

I did post a poll just now in the Discord. I'll end that in a minute, but go ahead and vote on the Discord and also go ahead and check in if you have not already. A few different things: so, we are going to have quizzes in this class. Snow days really threw me off, and the Martin Luther King [holiday] and everything—that shouldn't have thrown me off, I knew that was coming—but the quizzes are generally speaking, well, they're closed-note, closed-book, closed-computer, everything. They're just handwritten. They kind of look like this; these are just two examples. So, last year, this is what I did: I did a true/false for the first one. We didn't cover MATLAB, so you obviously won't get any MATLAB questions. I might throw one in there just for the heck of it, but they're generally going to be kind of simple questions that if you've been following along with class lectures and doing the assignments, they should be relatively easy. I'll throw these past quizzes in the drive so you'll have a benefit the last year did not.

But the quiz is coming up; it's going to cover basic Python and Git concepts. Nothing that we're going to be doing today, but anything we've covered in the last four lectures is a possible thing for you to have see on a quiz. I highly recommend that you take the summaries that are posted in the recordings, right, and just make sure you have that knowledge in your head—at least what's "quizzable" knowledge. And so I'll leave it up to you to figure out how you can study for that. I highly recommend you say, "Hey AI, give me the 10 questions from these summaries" or something like that. Start flexing those muscles on, if you're not already, as far as like using AI to study so that you do well on the quiz on Wednesday. So we are going to have a quiz this Wednesday. It's going to look very similar to this. I typically do them at the end of class, give you more than enough time to finish, and so typically I end class about 20 minutes early so that we can do the quiz and then you can leave when you're done. Sometimes this can take two minutes, sometimes people take the whole time—I don't know what they're doing—but it's usually true/false or multiple choice, so pretty straightforward if you know the concepts, and if you don't, you're pretty much going to miss it. Any questions about the quizzes?

**05:00 - 10:00**
*Student:* Are they on a scheduled date?
*Instructor:* Generally speaking, they're going to happen soon after the assignment is due. So usually the day after it is due. And that just allows—people have lots of reasons why they end up not submitting their assignments on time or whatever—but yeah, the quizzes usually are going to happen the day after the assignment is due. So hopefully this is not going to throw anybody for a loop. Remember you do have the summaries that are in the drive; you also have the PowerPoint notes. So anything that is in—that I've said—or anything in those PowerPoints or anything that's in the assignment, all that is fair game for the quizzes. But again, it's kind of basic knowledge-type questions, so you're not going to get any curveballs like "program this" or anything like that. Might have to do some code tracing or something like to figure out what this code does, but generally speaking, people do okay on these. Any other questions before we get started for the day?

All right, let's see what's going on with the poll. We've got 31 total votes, so let's end the poll there. It's okay if you didn't, just kind of wondering. Oh, interesting. So you guys can see that—so were you successful? So a lot of people, but we've got a decent "no," some did not try, a few people did not have your M5Stick. So you can see that. By the way, when you ask a question now, it will notify me. I wasn't getting notifications when people used to ask, and now I do. So feel free to use that if you have any questions, and I will do my best to answer them in a timely manner.

But today we are going to spend some more time with the M5Stick talking about C and C++ programming, probably doing a little "vibe coding" with some AI agents to see how that works with something like Arduino, which I think is actually really exciting, and just kind of showing the development environments a little bit more so that you get a good feel for them—where things are, what's sort of going on underneath the surface, things like that. And so I kind of glossed through a little bit of this last time. The full schematics—I highly recommend that you look at these. I don't know if this is big enough where I can zoom into it... it actually kind of is. But these are the schematics. I don't know if this is the M5Stick-C-Plus1 or 2, but they both look very similar to that. Something to really highlight here... I guess this is the Pico V3, so I think that means this is the M5Stick-C-Plus1. I can tell if I see something else in here... there's a power chip in the M5Stick-C-Plus1 that's not in the 2.

But the ESP32 is sort of like the brain. These are very much like your PC that's sitting in front of you, actually. They have a display, they have a processor, they have memory, they have IO, right? Like they have buttons and stuff akin to your mouse or keyboard or whatever. So they're extremely similar to what is sitting right in front of you; it's just very little and cheap, right? And that's about the only differences between them. They run a 32-bit processor; most of your processors are actually 64-bit processors. What does that mean? That means that the actual data pipelines for them—usually it actually means like the more about the memory addressing is 32-bit or 64-bit—but it could also mean that the actual pipeline for processing data is that many bits wide. And so it means something like you can add two 32-bit numbers or two 64-bit numbers together usually in one cycle inside of the chip. So as it's processing, each time it can do a single addition of those two size numbers. And so this is a 32-bit. In years past, we used 8-bit, and that doesn't mean you can't work with larger bit-sized data, it just means that you take multiple cycles to complete those operations on a narrower data path.

**10:00 - 15:00**
And so if you haven't taken like Logic Design, that's what you kind of learn in Logic Design and then going on forward sort of more about how the chip actually does computation. But again, 32-bit processor, it's actually a dual-core processor in both of these. That means they can run two simultaneous... not operations, but two simultaneous like lines of execution, basically. And so you can have two programs running essentially at the same time, or one program with two separate tasks running exactly at the same time, and then those two things can coordinate with various mechanisms. And so it's pretty powerful, though when you're programming with Arduino in that development environment, what ends up happening is that just one of the cores is typically used. And you can use another one if you want to; you just have to start that task through a separate operation.

But anyway, you've got your brain inside here, the ESP32 Pico. You can see it's actually hooked up to a bunch of different things. GPIO stands for general purpose IO, and there's a bunch of other hardware in there like resistors and things like that. These are generally known as pull-up resistors that make sure that the output of the or input of the pin is in a certain state and it's not just left floating; it's just whatever the ambient voltage level is relative to its ground. And so these things help programs sort of... well, typically they're used so that communication only has to ground a pin instead of also having to drive a pin high. But there's a decent amount of reasons why it might be one way over another way.

Some of these are actually like capacitive sensing pins. We might play around a little bit with that. What that means is that you can literally hook like a wire to the end of them and touch them and get an input. So when you're touching it or not touching it, you'll get a high or low. And so the ESP32s typically have those types of pins on them, and it's just an internal circuit to enable capacitive sensing. I'll probably talk about that a little bit. Again, you've got some buttons on here. This is all diagrammed in the schematic. And so these buttons that are on here—the front button and then you actually have two side buttons, side button on the left side and sort of bottom left and then top right—and all of those can be programmed for. The bottom-left button, by the way, is sort of your power button. And so if you... it depends on the chip. Some of them don't actually power down when you're when you have the USB plugged in. And so when you disconnect it—this one's running just sort of like a test program—when you hold down the power button, it will actually shut down. And so you can kind of see that the display is off. You hold it up close, it does have a backlight on it, and so you hold it down again to wake it up. Okay, so it can be turned off. It doesn't need to be charged all the time, right? It's not really a problem. They charge up pretty quickly and so you can kind of just let them die; you can throw them in your bag. They're not going to burn up your bag, hopefully. Maybe turn them off if you're worried about that.

But again, they do have a battery in them. They are completely independent. If you're wondering where like the radio is and stuff like that, that's inside the ESP32. And so like the Wi-Fi and Bluetooth, that's all inside that chip. That's one of the things about an ESP32: they're built for the Internet of Things, they're built for smart light bulbs and thermostats and things like that. There's not a whole lot inside. There's an accelerometer and gyroscope that is... this is the USB interface, I believe. Accelerometer/gyro might be on the next page on the actual schematic. And so that is a separate chip, not inside the ESP32.

**15:00 - 20:00**
But again, the schematic's there. Why you might ever want to look at this is that these pin numbers matter. So when you're writing programs and you're saying, "Oh, I want capacitive sensing from this pin," you need to know which pin that is. Now, there's sometimes libraries that help you with that and make it a little more convenient to just say "grab it from a particular pin" that's actually exposed here. Not all the pins, by the way, are exposed on the ESP32; some of them are internal, some of them are just not connected to anything, which is fine. And then some of them are actually exposed. And so if you look at the back of this, it's a helpful label on there—there should be a helpful label on there—that tells you what these pins are. There's also another set of connectors down here.

I mention all this, and then we're probably not going to ever use any of it because we don't have any hardware in the class. But you're welcome to hook up some hardware if you have it. Maybe I'll distribute something at some time. A simple wire is a piece of hardware, or a resistor is a piece of hardware if you're using the capacitive sensing. But you can hook up a lot of different things. They do have analog-to-digital converters on them, and so you could hook up a device that outputs a voltage or you could create a circuit that creates a voltage and you can do analog sensing, like touch sensing and things like that, force sensing, or you can drive various things. These do not have a ton of capability of driving output. They do have a lithium polymer battery which does give you the ability to potentially like drive motors and lights and things like that, although you would need some external circuitry to do that. Typically a microcontroller does not contain like actual high-current output circuitry, and so you'd probably need to add, for example, a transistor to it to drive a motor or something. So don't try to just directly drive things, including speakers, from these. These do have a little speaker in them; it is only really at one frequency—it's more typically known as a buzzer—and so you can get a little wiggle room around that frequency and so you can make different tones, but it's not a very great range. It's not like a typical sort of magnetic speaker.

Alright, so hopefully... I know a lot of people did program it, of the people that are here probably 80% actually did get the M5Stick program running, and so it is possible. What I should have asked is, did you get it working on a Mac or PC? I know my instructions are a little bit clearer for the PC because that's what I'm using. On the Mac, the only weird thing—and Mac users tend not to be really fully aware of this as much as the PC users because Mac just doesn't talk about it as much, I guess, or doesn't expose it as much—but the serial ports on Mac sort of have like a funny naming convention. Where on Windows it's like COM1, COM2, which is a very sensible name; on Mac it's like `/dev/tty.whatever.whatever.whatever`, right? And so it looks a little bit strange relative to PC, but it is ultimately the same thing: a hardware serial port that is an emulated serial port that runs over the Universal Serial Bus, which has really no alignment between those two. But this, when you plug it in to a computer, looks like a virtual serial port that you can talk to, and so you can send and receive bytes to it. And that's how it's programmed, and it can also be used as communication pathway for programs to talk back to your computer. And so that's actually at the end of this lecture where you can write a program on Python, you can write a program for the M5Stick-C-Plus, and you can have those two programs talk to each other over the USB cable. We can of course write programs that talk over the Wi-Fi or over Bluetooth as well; we'll be doing that in class. And so you don't have to have this hooked up to your computer; it does run that program completely independently all by itself. You can actually hook up the serial lines that are—may or may not be exposed on here—I actually don't think they are exposed on here, but you can actually turn two of the general purpose pins that are on here into serial lines and you could talk to another microcontroller, you could actually talk to another M5Stick through the lines on here. And so there's definitely a lot of possibilities for interfacing on this device. But to program it, for the most part, you do need to plug in the wire. It is technically possible to program these things over the air, over Bluetooth or over Wi-Fi even, but to do that, you actually have to be running a program on the M5Stick that accepts programs that way. It's sort of a weird thing; it doesn't always work that well. So the wire is a lot easier and generally faster, just more reliable. And so that's how I recommend that you program these, even though technically it's possible to have a workflow where you are able to program them wirelessly. There are many microcontrollers actually—a lot of Bluetooth microcontrollers are just programmed via Bluetooth and that's it, they don't even program via wire.

**20:00 - 25:00**
Alright, so let me jump back over here and continue on here. So we talked about serial port a little bit. Hopefully you all followed this. Probably the trickiest part of all this is getting the board to show up in the first place. It involves going into the Arduino environment, then adding a boards URL and downloading those boards URL so you get the actual board—the information for the board—which then sets up the whole toolchain. And so something to realize about Arduino is just sort of a front-end; it's really an ecosystem of boards that can be programmed or can accept Arduino programs, and that toolchain then adds infrastructure around for a particular microcontroller and how that microcontroller is actually hooked up to all the interfaces that Arduino sort of expects. And so, for example, Arduino expects that there's something called "Serial," and Serial can be used to send bytes. Where is that actually hooked up? What pin numbers are driven and things like that? And so the idea of building like an Arduino board—like let's say you started a company and you wanted to build a new Arduino—well, you'll have to provide a toolchain around that that lets the Arduino development environment and programs written for any generic Arduino actually work with your board. And so that's what that boards URL thing is: you enter that in, you find the M5Stick boards (really the M5Stack boards), it gives you sort of all of them, and then when you plug this in, you can then tell the development environment, "Hey, I'm actually programming an M5Stick-C-Plus2" or an M5Stick-C-Plus1, and then it knows how to program it.

Alright, so let me show you what that looks like in Arduino. Some people have already tried this, but I'm going to go through and just talk about it. And so this is Arduino. It might look like a light theme or a dark theme, doesn't really matter. Particular version of this actually doesn't matter a whole lot; generally speaking, looks the same. And so this is probably a little bit older than the one that you have. And remember there's also a possibility of just programming this directly on the command line; there's a possibility of doing this all in Visual Studio Code. You can never touch it and "vibe code" the whole thing if you want to—that actually works, believe it or not, because of the Arduino CLI. And so over on File, though, typically under Preferences, you'll find sort of the things like the boards URL and stuff like that. You gotta scroll down in this case, but "Additional Boards Manager URLs." You can actually put multiple in here. So I have the ones from Expressive ones, I've also got the Seed Studio ones, I believe that if I click here I can see them all. So I actually have several of these. They're not all technically necessary. Actually, the M5Stick boards are listed multiple times, and so you'll see that I actually have multiple listings for these. And technically they are different toolchains, but they should work identically. It's just sort of where does Arduino get the compiler that it then uses to compile the boards. Like what does it actually do to compile your programs, what does it actually do to get these programs on the board? Right, so it's the whole what I would call toolchain, from the compiler to the actual thing that loads the binary on the chip, right? And then it's all these additional files that are around there—libraries and things like that—that are included with that particular board that make that board work with typical Arduino programs. So when you get that boards URL, you're getting sort of all of those things, but you have to enter that in there. This is probably the one that you used, but actually several of them do work. And so I'm just going to cancel that out because I already have it. But once you do that, sometimes by the way, when you enter these things, you have to restart the Arduino IDE. It is not the most reliable thing on the planet, and so you might find yourself shutting down Arduino and loading it up again if you're using it at all.

**25:00 - 30:00**
And so I'll show you a few different ways that you could do this today. But this is the basic way, and this is a basic Arduino program. This is a C/C++ program. It has these two functions that are required if it's an Arduino program called `setup` and `loop`. `setup` runs once at the start; `loop` just runs repeatedly. And so if you're familiar with Python programming, which you are, what this would look like is a program where at the top of the Python program that would be where `setup` is, right? And then it would be sort of `while True:` and then whatever's inside `loop`. Okay, and that's going to run over and over again. The `while` part is hidden here. `loop` actually runs repetitively; it's just a program that's like `while True: run this code`. And so that thing that's inside the body of the `while` loop is `loop`. It's designed to make it easy so that you don't have to write those parts. You can actually completely ignore `loop` if you want; you could do something like `while True:` in this C++ code and run your code in here and it would run exactly the same way. You can just completely ignore `loop` if you didn't want to; it doesn't have to run, it just won't. Nothing really happens between `setup` and `loop`; it literally is just designed to make things a little easier.

But I see people running weird stuff in here where the `loop` function actually just runs a loop inside it, which is sort of totally redundant. But again, this will run repeatedly, and so any code you put in here will run repeatedly, alright? Kind of the most basic program you'll ever see written in Arduino is something like `Serial.begin()` and this sets what's known as your baud rate for your serial port. This is the speed at which it communicates bits over the serial lines, which are literally two wires, RX and TX, and it raises them high or low at a specific rate: the baud rate. Technically it's the symbol rate, but when you're talking about binary—don't have to be talking about binary, but when you're talking about binary—that is number of bits per second it is able to send. Not number of bytes, number of bits per second, and so 9600 would be 9600 divided by 8 bytes per second. So typically on most controllers today you're going to do something like 115200. These numbers look weird; they have a lot to do with typical frequencies that processors actually operate at. And so if there's no alignment between the baud rate and the processor clock speed, the serial port can do weird but funky things. The slower this is, technically the more reliable serial communication is. It's literally this timing thing and it's typically sort of this asynchronous thing where both sides agree on the speed as opposed to me actually being able to signal how fast things are going.

USB is able to do that much better. Now you might ask why don't we use USB? Well, USB is a much faster, much more difficult thing to actually run. I know this thing actually does that, and you can communicate using USB with a program that actually is designed to do that. This is very much like this backwards compatibility thing: everything in the microcontroller world pretty much uses what's known as this UART (Universal Asynchronous Receiver/Transmitter) or COM port or Serial port—these are all sort of synonyms. And so it can operate at pretty high speeds. This one can operate, if you know, probably up to two or four megabits per second, which serial... the USB port on here probably maxes out because this is mostly USB 1, at about 12 megabits per second. And so you're really not losing anything using serial. But again, mostly backwards compatibility. I'm not really aware of any microcontrollers that just sort of natively do USB; it's not very typical.

**30:00 - 35:00**
All right, so `Serial.begin(115200)` is a good choice, so that's a good number of bits per second. Every statement in C++—or every statement in C++ that's not a multi-line statement—ends in a semicolon. You don't have to write any of this stuff anymore or check it anymore because AI will make your programs perfect. And so I'm not really worried about that. But C++ code doesn't look like Python code really at all. We're going to talk a little bit more about C++ in a second, but I want to show you sort of a running program. And so then it would be `Serial.print` or `println`. Sometimes `println` just adds a newline at the end. `println` also flushes the serial buffer. So technically what happens is that microcontrollers are themselves this sort of composite of different pieces. So what tends to happen is that the processor actually puts—so the program puts a byte or puts whatever you put in the `println` in here into a buffer, and then that buffer gets sent out over the serial port over time. And so the print itself happens almost immediately. So this, the processor runs this and moves on to the next statement. And then over on the side it's kind of like putting something in a mailbox—that thing eventually will get sent over the serial port.

And when that actual process actually happens is a little bit variable, tends to wait until the buffer is full enough and then goes because otherwise it would have to... it's probably a little harder to describe. But suffice it to say that it will not send immediately if you do not force it to send immediately. And so `println` will flush it. You can also run `Serial.flush()` to flush it. These things matter if you're using the serial port for like communication of and you don't want a lot of latency on it. And so if you need to, you can flush, and that'll actually stop your program and wait for the serial port to finish sending before it continues with your program. And so if you wanted to force that, you could write `Serial.flush()` just like that. And then `println`, we can print something out, we can just print "Hello World." And this will repeatedly run "Hello World." Now, with the `Serial.flush()` here, this is actually a good thing; it'll print "Hello World" as fast as it possibly can at 115200. In fact, you'd probably time it and you will see that it will print whatever number of bytes that is, right, times that by eight and then divide this number by that, right? And that's how many prints you're going to get per second basically because it's running at that baud rate.

But from here, we can see that it's trying to program an M5Stick-C-Plus board. This is actually an M5Stick-C-Plus2. First lesson about these devices is it has no idea what's plugged in. Alright, no clue. And so all it knows is a virtual serial port. In fact, it thinks that it's a LilyGo T-Display. So it tries to figure out what's connected, but oftentimes it just fails because there's an ESP32 in there. It's based on sort of like what is the generic profile or most likely board given the USB chip it actually sees. Right, and so it thinks that it's this LilyGo T-Display. How do we fix that? We can select another board and port. If you do not see anything in here when you plugged it in, one of two things is going on. The first is that you might not have this turned on. So that would be why it's not showing up. The second is you might need to restart Arduino. Okay, so remember restart Arduino does help things many times. And so if you don't see anything here, you should see a COM port or the `/dev/tty.whatever` on Mac, then you might have to restart some things or turn on your device.

But I'm going to select another board and port, and you're going to get this list that shows up. So the first thing is select the COM port. If you have multiple of these, you need to figure out which one it is. You can do that if you go to like Device Manager or I guess on Mac you would go to `/dev`... it's a little bit more complicated. You can also go and like your system description and look in there and see what's connected in there. But when you disconnect it, the serial port literally disappears. And so you can see which one disappeared. And so on most laptops, though, you're not going to have multiple serial ports, it's just going to be whatever COM port. It will switch when you like change boards. And so if I plug in a different M5Stick, it'll be a different COM port. If I plug it into a different USB port, it might be a different COM port. And so this is just COM3 on here, but don't expect it to always be COM3 on Windows. And so I'm just going to say find the board here. So if you did the boards manager URL, you're now going to be able to find the board. If you don't see the board here, you're not going to be able to find the board. And so I'm going to search for M5Stick, and I want to do the M5Stick-C-Plus2. Remember we see two of them; I'll take the one directly from M5Stack, but I could also choose the ESP32 one.

**35:00 - 40:00**
All right, now this is actually important to do, even if you're going to use the Arduino CLI, because all of the sort of like preferences and all that stuff is stored in sort of a standard location. So regardless of which version of like the Arduino CLI you use—well, you should be using roughly the same one—but it'll find these things even if you... but if you just start with the CLI, it's a little harder to actually enter all this stuff. You can still do it, it's just a little bit more of a pain. And this is also the standard way to do it. And so again, I've got the M5Stick-C-Plus2 on here, right? It should say this before you start to program it. Why? This is the toolchain it is using to compile your programs, right? Turn them into binary, a binary blob basically, and then the tool to actually load it on the chip. It's just not going to work basically. You can compile your programs with whatever you want, just not going to actually get on the board. It's probably not going to break the board, but it's just not going to get it. Okay, so if you choose the M5Stick-C-Plus1, for example, it's literally programming, writing a program for a different microcontroller that has different pins hooked up. So it might program and just not do anything or do something weird. So you need to make sure you're choosing the right board so that it actually programs. There's two buttons in here: the verify button, which is kind of a stupid button honestly, because this button also verifies and builds your program. And so I always click this button, this upload button. And it takes—it takes a hot minute for this to happen. And depending on the state of your laptops, it might take quite a long time. Should go reasonably fast on my computer. Depending again on your laptop, it can be pretty slow.

Now, this one should go fast on most of your computers because I did not bring in any libraries. Okay, so this is generic Arduino program. It's going to use some of the libraries from M5Stack sort of that are baked in automatically to this; it's a little more obvious what those are—I'll show you in a minute in VS Code. But building a program, you can actually see it is also writing it, right? So there's a whole process that's writing, building, writing, and then loading. And this is now running on here, and my display is now black. Why? They didn't write anything to actually drive the display on here. So where is that? Well, it's being output over the serial port. So this is sending data, still sending data over the serial port. So just because I disconnected it doesn't mean it's not sending data over the serial port—still is. And so I can plug this back in here and then I can use a program that visualizes what's coming out of the serial port. Well, Arduino has one of those built in there called Serial Monitor. We do not have to use Serial Monitor; we can write a Python program that reads what's coming in over the serial port.

Note about serial ports: only one program on your computer can have them open. And so let's say you have your Python program running and it's reading from your serial port, Serial Monitor's not going to work. You can't program it, not going to work. You can only have one program have the serial port open at one time. So I'll look at the Serial Monitor, and we see "gobbledygook." Why do we see gobbledygook? Because your computer does not know the baud rate that this is sending at. Is it sending at 9600 baud? No it is not, it is sending at 115200 baud. And so it's trying to interpret the characters at 9600 baud and there's eight characters or so, or maybe more than that—16 characters—inside each one of those. And so let's change that baud rate to 115200 and there's our "Hello Worlds" that are printing out very, very fast.

We can slow those down easily enough in Arduino, but I think this is good to show because this `Serial.flush()` will actually sit and wait. If we didn't have the `Serial.flush()`, what would happen is it would keep putting stuff in the buffer, keep putting stuff in the buffer, keep putting stuff in the buffer, eventually the buffer gets so full it has to stop the program and send. And it does this weird thing, you get lags and things like that. So `Serial.flush()` ensures that what you actually put in there gets sent immediately and your program waits, so it's sort of a stall basically in your program. Wait for that serial data to actually get sent. Very important for IO over serial if you need it to get sent right away. Like "I pushed the button and the bomb explodes over there," right? So if you don't have that flush in there, the bomb will explode but just sometime after you press the button. Now, we're not talking about like minutes or anything like that, it's still pretty fast, but it is some delay.

Okay, so again, there's no way to stop these programs. They just—they're just happening. You can certainly like write something to pause your program or something like that. There's no need to stop them; they just run, they're just operating. All right, so again, can disconnect it, you can even like reconnect it on the fly and it'll just sort of start up. It's not starting the program, the program never stopped; it is just running. Okay?

**40:00 - 45:00**
Lot of misconceptions people have about these devices, so I know some of this is... I keep repeating myself, but the misconception that somehow the computer is running the program on here. This IS a computer. It is every bit as much a computer as a laptop is, right? For its own independent program. We could flip this and like "this program is that," which would be super weird, but it is a computer, okay? It is independent, it has a battery, it is doing its job regardless of what is connected on here. You can even hook up peripherals and stuff like that. You can hook up a mouse up to this and use a mouse and write a little mouse cursor and drive to the display. It is a computer, okay? Host mode, by the way... so this is not in host mode, this is designed as a peripheral and connects to the computer, but you can actually flip it and turn the USB port into host mode and then you hook devices in there. Hosts power their devices; that's sort of the delineation between a host and a peripheral is the host is responsible for powering the device, which it can do.

Alright, so let's talk a little bit about C++ now. So you should be able to get your program running. Remember boards URL, then you need to be able to select that board and see it, and then you just write a very simple Arduino program. You don't have to put anything in `setup` and `loop`; it'll compile without that, but just you won't see anything, nothing will happen. So having that little "Hello World" in there is a good thing. All right, this is all you need to know about C++, one gigantic block of text here. C/C++... and really it's Arduino. The language of C++ is actually quite simple in practice. All programming languages are actually really simple; they are not complicated. How to do things with them can be complicated, but most of the complexity in programming ends up being the particular thing or environment you're programming, right? The packages, libraries that you're using. Essentially it's vocabulary, right? You learned how to form words out of your mouth at a pretty young age, right? But it takes you... many of you are still learning, you know, how to form the right words, right, to communicate effectively. And so that is essentially what programming is in a nutshell: is communicating in particular languages to particular devices, right? Learning all the peculiarities of those devices and the constraints that you have and stuff like that.

And so C++ very, very similar to Python, which is very similar to almost all other languages. You do have weird languages out there—don't get me wrong—they're all building to the same device, so ultimately all running the same... on the same thing. Programming languages, if they are a programming language—like a general-purpose one—all can do the exact same thing. Now, just like human languages, a lot of them are similar and a lot of them are weird and different, right? Or different from you, right? So you have sort of like your languages that you're going to find sort of like in Europe, and you're going to find your languages over in Asia, and they're going to be different, right? And there are languages like that. I would say like Python, C++... like those are your familiar languages. They're very, very similar. They have this flow, there's `while` loops, right? There's `for` loops, there's `if` statements. You're going to find those in those programming languages, whether it's MATLAB, C++, Python, Java, JavaScript... they're all sort of built from the same tree. Then you've got other sort of like more functional, weird languages that everything's a function, it's big compositions of functions and everything looks kind of strange, and then you have those like functional languages that are sitting over there on the side. You now have like visual programming languages, which they're literally just like a graph—like a... you probably seen those if you've ever done like any like LEGO programming or something like that. Those things are, you know, they're visual, they can still be mapped typically like a Python/C++ thing, but they look very, very different. But again, they're all doing essentially the same thing, and a lot of them have similar facilities. And so again, C++ actually kind of similar to Python; really, Python more similar to C++. They're somewhat interoperable, by the way; you can write a C++ program and then the Python program can run the C++ like functions, right? Really not vice-versa, that doesn't happen too much, but a lot of times what you're actually running in Python IS a C++ program or a Fortran program, right? At the binary level, it doesn't matter that much, right? It's all running on the same actual processor. And so the programming language that the actual binary, the instructions for the processor were written in, right, that doesn't really matter all that much. Okay.

**45:00 - 50:00**
So some operators similar, similar expressions, things like that. And so like the asterisk is multiplication, for example, when you're writing out sort of a literal expression. Comments start with `//` or you can do this sort of like multi-line comment, right? You can learn all this stuff kind of independently as you need that. The things you need to know is that yes, you can comment things. Yes. Question back there?

*Student:* [Inaudible question about program on the stick]
*Instructor:* Oh, the original... oh, you're like, "Oh, where did my pretty program go where I was able to hit that button and stuff like that?" Yeah, so good—good question. If you go into M5—like all so where I posted that link where you can find sort of information about it—they're going to have this like UI Flow or whatever, like there's this environment that you can download that you can put programs on here that other people wrote. That came from that. And so it's not like an Arduino program that put it on there; it's actually from there and it's just like a binary blob that will go from that onto the M5Stick. Yeah, so if you want that original program back, I know that thing is fancy and neat and stuff like that. It's incomprehensible from a code standpoint; there's a lot going on in there—I mean, like many multi-hundred line programs, like it's just hard to understand. And so it's cool that it's on there and you can build all those things, it's just it's a compilation of programs designed to show you what this thing is sort of capable of. But yeah, you can get it back; it's available on M5Stack's website. We are not going to use that environment that's on there.

So again, you can do things like multiplication, division, exponentiation, things like that, although C++ doesn't have a direct way to exponentiate like with a literal statement, you have to use a function for it. The big difference between a programming language like C++ and a programming language like Python is that C++ is almost always compiled, Python is almost always interpreted. What does that mean? Python requires a program, typically written in C++ or really C called CPython, right, really it's a sort of a framework, to actually execute the Python program in real time. So it's literally a set of text that's going through an actual program that then runs on the chip.

C++, you are building that program and then putting it on the chip and it's running directly. Okay, so they are typically compiled, meaning your program that you write is turned into machine code which then directly runs on the hardware of whatever architecture you built it for. You can build programs, obviously, on your computer for a different architecture. You probably have an X64 architecture, maybe you have an ARM if you're using like one of the newer MacBooks (the M-series, those are ARM processors). You can cross-compile, you can build for a different architecture, this one being this like Xtensa something, it's sort of a... it's not ARM, but it's I think it's an Xtensa, I think I have it written somewhere over here. But anyway, it's an architecture that, right, has its own instruction set architecture, it has its own compiler, which is essentially the process of saying, "Here's a language," right, C or C++ in this case, "turn that into binary blob, right, a set of bytes that is essentially instructions," and there's a loader program—actually the loader program is written in Python—the loader program through USB talks to a program that essentially is running on here independently sort of like running underneath the surface that actually takes the takes the program and puts it in the right spot in memory and then when you resets starts running it actually on the the bare metal of the of the chip.

It's a little bit different than how your actual computer works. There is an operating system, by the way, in here. It's called FreeRTOS. That's actually what the programs are communicating with, that's how your programs are being loaded and stuff like that. But FreeRTOS is very, very different than what's running on your computer typically. Your computer general-purpose operating system run like 75 different programs, right? And this typically they run one program, and it can have multiple tasks and stuff like that. But real-time operating systems typically are running one program and they're called real-time because they have real-time guarantees. On your actual computer, it's very hard to have any real-time guarantees, meaning everything is going to something's going to happen every 42 milliseconds without fail. This, something can happen every 42 milliseconds without fail until the end of time. And so real-time is very different, a very different set of constraints that you need something to happen exactly when you say it's going to happen, there's nothing else running on it, and so nothing else can interfere with it. But it does have some facilities like running tasks and things like that, like running parallel tasks if you need it, and so it does have an operating system running underneath the hood.

**50:00 - 51:00**
So again, the big difference is it's compiled. Compiled programs—not all of them, but many of them—also force you to specify the type of thing that a memory address is being used for. So when you declare a variable or declare a function—meaning you're writing a program saying, "I want this variable"—in Python you just say like `x = 4`, right? Underneath the hood, Python is doing something with that and putting it somewhere, and you just essentially don't need to care when you're writing the program. In C and C++, you actually have to specify what type of thing that variable is. Now, that's not always 100% the case, in fact, C++ does have sort of like automatic type assignments. The difference is in Python, a variable can be assigned to anything and it can change as your program runs. So `x` can be an integer in one case, it can be a float in another one, it can be a string in another one. Why? Because everything in Python is an object, and so anything can be assigned to any variable.

In C++, variables have one type for their entire life, period. So even though C++ does have automatic type assignment, you literally have to say like `auto this = that`. It'll automatically figure it out. More typical way of writing things out is to specify the type: is it an integer, is it a float, is it a char (a character), is it a char star, which is character pointer, which is anything with a star is technically the same thing. The thing says like what is this pointing to, or really what the first thing this is pointing to. Char star says "this is a memory address that that memory address holds characters, right, typically a sequence of them." And so a sequence of characters is a string, so that's why a char star is a string. C++ also has objects, they also have string objects, and so char star is more of a C terminology. It's still very widely used in microcontrollers.

Objects still happen, so like in Python, you can create sort of like new objects and those sort of have composite structures. Python typically is going to use a lot of what's known as dynamically allocated memory, and so I can create a list in Python or I can create a string in Python (maybe not string is a bad example), I can create a list in Python, I can just kind of add to that, just extend it. That really is not the case in or not a good thing to do inside like a microcontroller platform that has a very limited amount of memory. Because what ends up happening is like "I want to add to that list." Well, if let's say I have two lists and I want to add to the first list, well, what happens if that list becomes needs to get, you know, needs more space? Now you got the list below it. Well, what ends up actually having to happen is that you need to detect it in the first place, otherwise it's going to literally overwrite the next list because it doesn't have any protection for it whatsoever. So what ends up having to happen is if you need a longer list, you gotta copy that whole list to another place where you do have memory that's available and sort of extend it—gets very expensive. But Python is just managing all that for you; you never have to think about memory in Python. You can think about memory in Python, but you don't have to. It just makes it happen automatically. Now, if you're not aware of it, it can make your programs really slow and things like that, but it doesn't break them fundamentally.

It will break them fundamentally in C or C++ if you do not... you're not going to like break the microcontroller or anything, never worry about that, that is not unless you're actually hooking something up to this, you cannot break it. Now if you hook up wires to it, you can absolutely break it. Then you're talking about electronics. This is just programs; programs really for the most part cannot break microcontrollers. They really can't break your computer either—there are ways, but certainly done them—but generally speaking, you cannot break your microcontroller by loading a program on it. Again, unless there's hardware hooked up to it, then you can absolutely break it.

So long story short, you have to be aware of a lot more things with C/C++. The big thing is types: declaring your variables as integers or floats or chars, things like that, and memory. Those are the two big things that you need to be aware of, and they're related, right? So when I say `int`, I'm specifying not that this is a number, a whole number, I'm really specifying this is a certain number of bits I'm going to use for this and interpret it that way. And then the compiler uses that to say, "Oh, if I add two ints, I run this function; if I add two floats, I use this other function over here or this other procedure over here or this other instruction over here." So the compiler's able to use that type information to run the specific actual machine code instructions to do the task. Python just makes that happen underneath the surface through dynamically evaluating your programs and figuring out what needs to happen based on the types of things. And as a result, it is very slow—I mean, many orders of magnitude. A typical Python program 100 times slower than a C/C++ program because has to check everything because doesn't know what's there as it gets there. C/C++ way faster because knows exactly what's there, it's been compiled in there, and it is just running; it is not checking anything. Doesn't have any type checks, doesn't have any memory checks. Those things can be built in as an add-on, but doesn't have them by default. As a result, it is crazy fast. In fact, your typical programs running on here in C or C++, even though this computer is way more powerful, a Python program would run slower to do the exact same thing. So this, in its little itty bitty using milliwatts of power, is going to be faster in C++ than a Python program running over here and more reliable, right? It's going to be more real-time. That's why we use these things.

All right, so I knew that was going to happen where a decent amount of the class would not have gotten things to work. Hopefully you are able to get there. If you're not, please come see me in office hours. If anyone does not still have one of these, you need to have one of these. I will add another assignment waiting for you for sure before Wednesday. It's going to be pretty rapid. Again, I feel like the programs in this class are able to be put together pretty quick. I'm going to show you on Wednesday how to use VS Code to do this, but you're welcome to try that as well, and we'll get to the vibe coding side. You're welcome to try that as well. It all kind of works if you go into something like OpenCode and say, "I want to program Arduino, what do I need to do that?" and it'll install the toolchains and it'll get it going. It's very nice if you already have the Arduino IDE set up, you already have the boards list set up through this, it goes a lot smoother. All right, so go around, play around with it some more. Hopefully that helps you understand what you're actually doing. But feel free to ask questions, and feel free to ask questions about the quiz that's coming up as well. Remember those summaries are there, not from today—nothing from today will be covered—but anything up until today is fair game for the quiz, okay? But it is going to kind of focus on Python and GitHub... Git, not GitHub. A little GitHub. All right, see y'all.