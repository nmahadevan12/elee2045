This is the transcription and summary of the audio: 

### **Audio Transcription**

**00:00 - 01:00**
All right. There must be some horrible traffic accident or something. I hope everyone's okay because there are six people in this class and there’s like 35 of y'all. So, we do have a quiz. It's not going to be until the end of class. I do that just to allow people who need extra time to have extra time. So, I usually stop with about 15 to 20 minutes left in class and then distribute the quizzes. They really don't take long if you are pretty confident in your answers. There's not really a whole lot of thinking you can do on them, unfortunately. But I'll talk about the quiz right before I distribute it and give you some strategies that you shouldn't follow.

So, today I'm going to do a few things. We're going to keep going through this lecture. We also have an assignment that's posted we’re going to go over. So, that's going to be using the M5StickC Plus as well as a Python program. And then I'm going to do a little "vibe coding" today, at least show you the vibe coding tool I prefer to use. It even works with running Arduino programs and things like that because it can sort of wrangle the Arduino CLI.

**01:00 - 02:30**
So, I'm actually going to get started with that just because I think it's pretty relevant. I don't know how much I'm actually going to use in class, but I do think it's a really good tool. So, the tool I really like is OpenCode. If you search Google—or I guess your favorite AI tool—for OpenCode... hold on a second, I gotta switch around the order of my monitors. It’s backwards and it's going to drive me crazy. There we go. Apply. Yes.

So, OpenCode, if you search for it... These tools that are out there now, I already mentioned one of them, Gemini CLI. There's OpenCode, there's Claude Code—probably the most popular one out there. I learned a new word yesterday: an "agent harness." And so, sometimes I guess these are now known, as of late 2024/early 2025, as agent harnesses. The agent being whatever underlying system that's being used—ChatGPT, Gemini Pro, Claude Opus, whatever it is.

**02:30 - 04:00**
But then there's this software that sits on top of it, right? That I previously would have called the agent in the first place, calling the other thing the LLM. But I suppose the tools they’re interfacing with are more sophisticated than just a straight LLM. So, this harness actually does make some sort of sense as a term. But the idea here is that there's a software process, an algorithm, that is driving how these tools are being orchestrated.

So, you've got this really powerful engine effectively in the LLM. These things are the car that sits around it. It's how it's all integrated together. You know, what happens when you push the accelerator? A lot of different things are actually going on. There’s a lot of monitors to make sure everything's working right and things like that. And so, this has kind of become where the companies are actually innovating because there's only so much they can do on building better language models. The real moat these companies are building is around their coding tools and these harnesses that they're creating to actually accomplish tasks in a more meaningful way.

**04:00 - 05:30**
OpenCode is one that I found through evaluating that because I don't like to pay for things. I'm sure you’re all like me; I really despise paying for things. And so, especially I don't like advocating for stuff that you have to pay for. And so, I found OpenCode as a really good competitor to Claude Code, which is arguably the best vibe coding harness relative to all the other ones out there. There's also Codex from OpenAI and Gemini CLI from Google.

Anyway, I really like OpenCode though, in that it seems to function really well. And I really love that when you open it up, you can actually switch what model you’re using. And so, you can choose from some free models that they provide if you want to use those. I have the Google Pro module installed. You can also use your API key with this. So, if you go to add a provider, you can manage these things and add a provider. I updated the software right before class started, so hold on just a second.

**05:30 - 07:00**
Okay, so we've got a little menu up here, we've got settings, models, providers. So, you can go to edit this. I have a custom Google connector set up that actually connects to my Gemini Pro subscription that I have. But you have all these different things and you can change them. You can use Claude, you can use Copilot, you can use an OpenAI model. You can actually use OpenRouter which then itself connects to a bunch of other things. So, essentially, if you have a subscription to one of these services or you’re using a free model, you can use this with OpenCode because it really is just the engine that is enabling what this thing does.

And so, how this operates is that you give it a folder, and then it can operate within the folder. Anything that's in the folder, it can use. So, you can have this large database of stuff and then it will be able to analyze that. How does it do that? Well, the most powerful thing these things have access to is your shell. And so, your shell meaning your command line—they can basically do anything that you can do within the shell.

**07:00 - 08:30**
That's a really powerful feature. So, you're not just asking it to generate text; it can actually use the shell tool and it can manipulate things. It can create stuff. It can analyze the contents of folders and do the right things, and then it can use all these tools that you give it access to. So, one of the really nice tools here is Arduino CLI. So, Arduino CLI can be used to build Arduino programs for specific boards, things like that.

Now, I do find that it is better if you've opened up the Arduino tool, you’ve set it up, you’ve done a build, because then you know sort of Arduino works. You know what port you’re using, you’ve got the board list set up, things like that. If you don't have those things set up, Arduino CLI is kind of a pain. But Arduino CLI is just a command that you can run. You can search for Arduino CLI and download that separately. You technically do not need the Arduino development environment at all. You can use Arduino CLI for literally everything.

**08:30 - 10:00**
So, you can just start asking it questions. You can say, "Use Arduino CLI to check the available COM ports and help me select the one for my M5StickC Plus 2." So, this is going to use Gemini Pro. Some of the free ones are perfectly fine like that GLM 4.7 is excellent. It should be able to do all of this no problem. COM3 is the only detected port, so it’s actually using Arduino CLI underneath the surface and it’s telling me COM3 is there. And it’s actually asking me a question that I get to answer. This is the whole harness thing. It’s sort of like, "Okay, I don't know enough, I need to go back and get user input." Where did that come from? That’s not from the large language model; that’s from the tooling around the large language model.

"Yes, it's COM3. Great." And it's thinking some more, identifying the serial port, using Arduino CLI commands. So, installed cores, it can see a lot of different installed cores. It’s got the M5Stack, which is great. That's exactly what we're looking for. So, it's doing an install actually of the M5StickC Plus 2. Fantastic.

**10:00 - 11:30**
So, now it sort of knows. "Let's build an Arduino program that prints out 'Hello World' every one second." So, it's going to build it. By the way, you can operate these things in two modes: "build" and "plan." "Build" will actually run commands. "Plan" will just tell you what it’s going to do. And so, if you’re scared about what it might do—which you should be scared about what it might do, especially if you’re working on some production system—you should probably plan first and then build.

So, it built this `Hello_World.ino`. One thing I actually kind of hate about this tool is there’s no file system. Like, you can’t see your files anywhere. That’s interesting. So, it's a good idea to actually have VS Code up and running here. So, let me go into my vibe coding example. You can see `Hello_World` is in there. And I'm going to open up VS Code in here.

And this will let me see what's actually going on. So, you can see it actually created a `Hello World` application. It created the folder, it put the `.ino` there, and it actually made an Arduino program that we can see. It used the right baud rate, it put the delay in there. This is exactly what I would have written. One weird thing in here: it gives you a comment, `while (!Serial)`. Serial port does actually take a second to spin up. And so, if you're going to do something like printing, the `while (!Serial)` makes it so that there is a serial connection before it actually starts printing.

**11:30 - 13:00**
You should always look at your code. I'm a big advocate for looking at your code and seeing what it does. In fact, I'm playing around with an idea for your next quiz where your quizzes are going to be based on *your* code. So, everybody gets their own quiz. This is all enabled by AI, right? But I kind of like the idea of, "Hey, if you're going to be writing some code in here, you take the ownership over it and you should be able to explain what your code does."

Let’s see if it actually runs. Let’s actually build it. So, again, very cool. You can say, "Okay, run this and make sure it works on my M5StickC Plus 2." Make sure you're doing the right M5Stick. Remember it won't work otherwise. So, it can actually run it. It can connect it all. So, it’s going to actually have to figure out how to open the serial port. So, this is actually another problem, right? How is it using the serial port? Apparently, there's an Arduino CLI monitor. It's saying the command line timed out, it doesn't know what to do. It’s actually debugging all of this and figuring that out. It’s all very fascinating to me.

**13:00 - 14:30**
So, it’s saying `monitor.log` not found. I've built programs on here; they've usually been in concert with a Python program and it uses Python for it. So, it can figure that out probably. Lack of output, process was killed. Let’s see. Ah, attempting further verification. It's probably building. So, it's actually going through debugging. It does what a human being would do, in my opinion, is that, "Oh, it's not working. Let's build like a test program on here to figure out what's going on and debug those pieces of the puzzle." It follows some very good strategies.

But it'll take a decent amount of time. It'll kind of go back, it'll run things. It’s actually building; you can see it just built a new version. We can monitor what it's changing in here. So, it actually inserted some delays in the code. So, VS Code will update. That delay 500 in the beginning is a little bit weird. Let's see. So, it’s monitoring it. Let's see if it’s actually getting anything. It would be interesting to see if this succeeds.

**14:30 - 16:00**
Oh, so it determined that the line is problematic because of buffering or timeout. The upload itself was confirmed. I know this code works. Fascinating. It's going a little crazy now. What is it doing? Woo! Interesting. So, eventually, sometimes these things go a little bit insane and you have to stop them. And that's perfectly fine. And so, I'm almost thinking about killing this at this point because it's going a little crazy. And what I'll probably do is tell it to build a Python program to read the output from the serial port.

"Write a companion Python program to read the output from the serial port." Could be a lot of different things why this actually isn't working, so we're going to go and try a different approach. And this is coding right now. It's like, especially with hardware—and this is kind of why I like this course—because a lot of the simple things are just going to fail. Right? These ideas of, "Okay, this can run, this can work," there's always going to be little problems. Maybe when you run this, it works, and you're going to have to figure it out.

**16:00 - 17:30**
It's going back, it's installing PySerial. It's running pip commands. You can tell it to create conda environments and use those. That actually works just fine. So, yep. So, it's building some things. We can see the program it's actually building over here. It's using PySerial. We're going to talk about PySerial in just a second, but it's pretty simple. So, it's actually building it, it's running it. You can see it's running it, `read_pyserial.py`.

There’s some other output. Flush. Oh, there output it! So, it says it's functional now. And so, what you typically do after this, it's saying it’s receiving it, but you can actually run it yourself. So, I'm going to go back over into the console here and we're going to go and just do a `python read_pyserial.py`. And so, you can say it’s receiving "Hello World" running on the M5Stick.

**17:30 - 19:00**
So, you can run some pretty sophisticated things. What I did was probably a little bit... it's not exactly what I would have done in practice. I was wondering what it would actually do if I just said "do everything." It's usually a better idea to work in smaller pieces. Right? So, okay, create the Arduino program, let’s run it, maybe open up Arduino, check it out yourself, right? Let's then create another program. I had never used Arduino CLI to monitor a serial port. That may or may not work. I have no idea. And so, that's where it failed, the thing I'd never tried before.

But anyway, it's able to code M5StickC Plus 2 programs. It can do a even better job with them than I'm doing right now. So, let's see. Let's try to change it a little bit. Let's say, "Make the M5StickC Plus 2 program display a spinning square. See if it can do this." So, this is going to end up having to, you know, figure out the graphics language basically, and figure out how to rotate something. It’s going to have to do some math in there, right? Lots of different things that are going to have to happen.

**19:00 - 20:30**
Let's go back over to VS Code. We can kind of watch what it's doing, what it's creating. It's still thinking because nothing's changed there. Yeah, so it's actually creating a new script entirely is that what it's saying it's going to do. So, `spinning_square`, thinking, going through all of this. Tons of actions happening. This is a very different process to your typical use of an agent where you go in and you say "make it" and it just makes the program and then it's your responsibility to test it and see if it works, right? Very different way of thinking, very different way of coding honestly.

You can also do this at some level from within VS Code if you turn on Gemini Agent. So, the Gemini tool, the Code Assist tool, does have an agent mode that can do some of these things. It's a different approach. I don't know which one you're going to like using. I think that it’s a little bit more deliberate to using an approach where you're just asking for code and you do the testing. Forces you to think a little bit more.

**20:30 - 22:00**
This can do some really sophisticated things though. And so, there is a tradeoff, especially once you feel like it has all the right tools available to it. And so, it's actually building it, it’s compiling it, it’s uploading it, and we’ll see if this actually does generate something that is functional. So, go back over to here, we've got our `spinning_square`. Here's what that spinning square program looks like. You can see it's actually using Canvas, which is the right approach, as we'll see in a second. I'm going to talk about that a little bit more. It's doing some... it's trying this `PushRotateZoom` thing, which it says exists. So, we'll see. I'm not actually convinced that that exists. And so, but it compiled, so it must be a function that exists. It reads documentation really well.

Sometimes you have to hand these things documentation because one of the problems with LLMs is they can be out of date. And so, they might be out of date by a year and something new comes out that you're trying to use or some library changes and you have to literally feed it documentation of the new stuff. So, let's see.

**22:00 - 23:30**
It's pretty good, right? There you go, you've got a spinning square on your M5StickC Plus 2. And there were a lot of commands happening to orchestrate all of that, right? So, it had to create Arduino programs, it had to compile them, it had to upload them. You've still got the Python program side that you could write for this. But these tools are very, very useful, especially if you have confidence in what you're going to be creating as something that is possible.

So, anyway, we could have gone in a different process again with this. So, I like these coding tools, I like OpenCode because I can literally switch it. I can say, you know, "use this cheaper model" for example, or "use this OpenRouter large free preview." That should work pretty well as well, right? So, I can literally switch it and say "make the spinning square purple or something" and "make it move up and down on the screen." So, we'll see if that actually results.

**23:30 - 25:00**
But in the meantime, I'll let it do its job and pop back over to the PowerPoint and we can discuss sort of more the theory of what's going on in a little bit more depth, okay? All right. So, M5Stick, lots of different functions available to you. I do actually recommend that you go and look at the documentation to sort of know what's available. You can also probably ask your various AI tools what functions are available in the M5Stick. This is a more sophisticated M5Stick program. We’re going to go through this line by line, sort of explain it all and talk about C++ a little bit.

One of the most important things is this `#include <M5Unified.h>` at the top. So, M5 is a whole set of boards. So, you have M5StickC Plus 2, there's also a StickC Plus 1, there's also an M5Stick direct without the Plus. There's a tons of other boards. There's an e-paper one, right? There's lots... there's an M5 Core, tons of them. And so, rather than having different libraries for all of them, they have one library called `M5Unified` that largely supports all of them. And where it doesn't, you can still drop down into the more specific libraries, but I found last year that `M5Unified` has come a long way and is really capable of doing this sort of cross-platform development without having to learn lots and lots of different APIs depending on which board you're using.

**25:00 - 26:30**
So, it's convenient for the class because some of us have the StickC Plus 1, some of us have the StickC Plus 2. And so, that `#include <M5Unified.h>`, what that does is it literally brings a text file into your C++ program. It is not the same as like an import statement in Python. It's a little bit different, but `include` is essentially equivalent to that in what you're trying to do typically. All right. So typically, you are trying to bring some code that you don't have, that you did not write, into your program. It might be something you wrote; it's just somewhere else, okay?

Now, where is this `M5Unified`? Well, it was provided by the M5Stack library. So, if you looked at the M5 library, you would find `M5Unified` and you could actually access that on your computer. It does live on your computer. It lives in a folder that has all of your Arduino libraries for the most part in there. It's sort of a hidden folder because you don't normally touch that. If you needed to touch that, then you would need to drop down and like you’d probably maybe like pull a git repository for an Arduino library that you wanted to bring in, you’d put it in that folder.

**26:30 - 28:00**
But the Arduino library manager, which you can use Arduino CLI for that—in fact, it already did use Arduino CLI for that—but if you look and open up your Arduino, you can actually see it a little bit clearer because it’s a graphical user interface and it'll show you all the different libraries that are available to you. And there are tons. This is actually one of the reasons I like Arduino because they have just a ton of user-created libraries that are available, community-created libraries. And so, if we search for M5Stack, you’ll see there’s this M5Stack that's already installed. That's where that `M5Unified` came from, okay?

And so, you can do the "more info" on here if you want, but this is a GitHub repository. Almost all of them are GitHub repositories. And if you were to dive into that, you'll actually end up finding the `M5Unified.h` code somewhere. Let's see... `examples`, that's not right. `utility`. I feel like `M5Unified` should be in here, but I could be wrong. Might be a separate library.

**28:00 - 29:30**
`M5Stack_WiFi_Uploader`, `M5Unified`, sorry. It’s actually here. More info, and then you'll find it in there. Right. So `M5Unified` gives you the ability to do that and you could actually see what that code was if you wanted to. You go into the source and look in `M5Unified.h`. All that does is include this `M5Unified_H3.h`, which it says it’s not found even though I clicked on it. That’s weird. And you can see this has a lot of stuff in here. This is all the code actually necessary to be cross-platform across all the different boards. Right. You'll see a lot of these like `#ifdef` defined this or `#ifndef` defined that and it's just for each board you're going to have different pins that are assigned and stuff like that. And so, it has to change it for each one. So, it's not something you're ever going to touch, but it's useful to know where these things are coming from, okay?

So, that's what that `#include` does. It's basically how you bring in a library. There's several ways you can actually format this with quotes or not quotes, but it depends on which directories it's searching. And so, the actual Arduino development tool is going to search particular directories for things. But this code should work in Arduino. In other C++ applications, you might need to put angle brackets around this instead if it's a system library.

**29:30 - 31:00**
And then you can see the standard `setup()` and your standard `loop()`. In `setup()`, all it does is this `auto cfg = M5.config();`. Okay. This is weird. This is an M5-specific thing. All tools are going to have their own weird ways they have to do things. All this is doing is saying, "Hey, I'm going to need some configuration potentially here." This next line, `M5.begin(cfg);`. What is this? `auto` is used so you don't have to remember the name of their peculiar class that they created for configuration options. That’s it. `auto` is a way of asking the compiler to figure out what the type is. `M5.config()` returns a very specific type of data, and so it knows the type of object this thing is or the type of variable that this thing is—object this thing is, and it assigns it, right? So, you get... you don't have to know it basically. Because normally you’re going to say `M5.config()` and `M5.begin(cfg)`. But if you wanted to know exactly how to use this, you would go over to the documentation, which I'll click on here. And it's going to find it. It'll open it up and we should be able to see what `M5.config()` does. We might need to go to some sort of reference for it. Sorry, it's all very small. No, it's probably going to be in `document`.

**31:00 - 32:30**
Let's go back. No, no, no, I don't want to do that. Gosh, everything is hard to find. Maybe it's under the "Getting Started." This is where I was trying to find. So, `M5Unified`, you can see `M5Unified` setup and the API. And so, within this we should be able to find the documentation on the `config` command. So, you can see assign a structure and then there's some things within the configuration that you can set. So, in this case it’s like `external_speaker = true`. So, there are various options that you can change in the configuration, like whether or not you want the backlight to be on and stuff like that.

Oh, by the way, I made it move up and down. It accomplished tasks just in the background doing its thing. That's the beauty of using these agents is they can actually make things happen. Whereas like a simple code editor where it’s just changing code, you have to make everything happen. You have to orchestrate everything. Not necessarily a bad thing, but if you want to save yourself some time because you know something is possible, it's actually a lot easier just to say, "Hey agent, go do your job." Come back 20 minutes later and it's done. That doesn't always work; in fact, it rarely works the first time. But these tools are getting quite a bit better. And so, again, if you know something's possible, you’ve done it before, you have a really good example or you have some starter code, agents are awesome.

**32:30 - 34:00**
All right. So, you've got the `M5` command to get a configuration that you can change if you want to, but `M5.begin()` is how the M5 sort of initializes itself. At least it’s recommended to you. You saw that we used this thing without using `M5.config()` earlier. We programmed it just like an ordinary Arduino program with serial output. That's available to you even if you're not using any M5-specific functions. But if you're going to be using M5-specific functions that rely on this configuration being set up, then you have to run this `config`, okay?

And so, normally your M5 programs will begin with these two lines of code. You can do things like changing rotations, right? You can do things like setting speaker volumes. So, this does have a speaker on here that does have a volume, and so it works with the `M5` command. So, this just raises it to the highest level. You're going to see numbers like 255 a decent amount in programming. What is 255? What is the... what is the meaning behind 255?

**34:00 - 35:30**
Yeah, so eight bits. The maximum number of values you can have in eight bits is 256 different values. But if you include zero, the maximum value, you know, positive value would be 255. So, you see things like 255 a lot because in the microcontroller world where you have limited number of bytes of memory, bits matter. In fact, they always matter. It’s something that you should appreciate as an engineer that is constantly going to need to optimize stuff. I don't care what platform you’re on, bits matter. So, it's always better to go 2% faster. Optimization is one of the things we do as engineers and we have to figure out how to squeeze every last possible, you know, millisecond out of the computation that we of our systems perform. And so, these things do matter quite a lot. And so, again, it just doesn't... just because you have the computation available doesn't mean you should just, you know, gratuitously exploit it.

Now, that being said, you have to really consider your time as well. And so, if this is something that somebody else is going to use, it's going to impact a lot of other people. It's going to impact everybody for that millisecond of time you didn't optimize or that you did. And so, if you're building something for yourself that’s just sort of a one-off that’s going to be a tool, who cares? If it's something that somebody else—again, you're distributing to millions of people—now all of a sudden if, you know, their system is running even a small amount slower, it does matter.

**35:30 - 37:00**
So, next part of this, we're actually using some M5 functions. If you didn't run this `config`, this may or may not work. It probably won't work. I know things won't work properly if you don't run `M5.update()`. So, `M5.delay()` is a delay that's built into it that is a smarter delay than Arduino's delay. So, `M5.delay()` potentially puts it to sleep and so potentially saves power. You could use `M5.delay()` if you want to; you don't necessarily have to use `M5.delay()`. You can use Arduino's ordinary delay. But `M5.delay()` does exist.

Then there’s `M5.update()`. `M5.update()` is a really interesting one that we could dive into if you wanted to look at the actual code for it. But what it is doing is it’s handling a bunch of state—state being values—that other M5 functions can potentially be modifying or other things that might be checking that. And so, `M5.update()`, what it actually does is it, for example, polls all of the electronics—the circuits that are feeding digital values that are going into typically the microcontroller pins—and it's polling them. For example, is the button pushed or not pushed?

**37:00 - 38:30**
And it’s doing that every time you call `update()`. It can use that to detect events that have occurred since the last time you’ve updated, right? And so, if you don't run this, you can't use the event functions, and the event functions are really useful. So, if I want to know if somebody did press a button at exactly the moment they pressed that button, how do I actually do that? There's a lot of ways to do that in the microcontroller world. But one of the simple ones is you can simply just check it all the time. Is it 0 0 0 0? Up, it’s 1. Okay, they pushed the button.

But you can't just keep doing that, right? Because then the next time it'll still be 1 for a small amount of time because they’re holding the button for a small amount of time. Remember, these things operate in the microsecond world, not the world that we live in typically where we have seconds of update. So, even if you feel like you just clicked it once, you just tapped it once, the reality is you've probably been checking that thousands of times, right? And that 1 is true for a thousand times. And so, you can’t just constantly make it happen. And so, you have to have another flag that says, "Oh, I already checked that and so I’m going to wait for it to go back to 0 again before I check it again." This is a fairly simple thing to write in practice, but `M5.update()` effectively handles that for you.

All right. And we’re basically at the time where I need to stop and distribute the quizzes because otherwise you’re not going to have time to take them. And so, I'm going to start handing these out and you're going to get a chance to take this. So, put away your stuff. I don’t think this is going to take a ton of time for most of you, though.

"Is it an open book?"
No, it's... well, it's written but you said you need a pencil or a pen. And it's multiple choice. It's multiple choice. So, read the instructions at the top. Hope for all of y'all have a pen or a pencil or something. I can see everybody getting anxious. It’s like, "must be quiz time." Pass it down if you don't mind. Here you go. Pass these down here as well. There you go. Got a couple extra? There you go. Pass those down there. Yep. Just to the end down there. Yep. How many you got? Two? Here you go.

The longest answer is not always the correct one, okay? Some people think that and sometimes it is true, but I'm very careful about it. There are no tricks on my quizzes. All right. Again, closed notes. And when you're done, you're welcome to sit in your seat for a little while, but when everyone else starts leaving because it's the end of class, you may turn it in. Try not to be disruptive. Pause this.

***

### **AI Summary**

**Lecture Overview:**
This lecture focuses on **AI-assisted hardware programming** (referred to as "vibe coding") and a deep dive into the **M5StickC Plus 2** hardware ecosystem. The instructor demonstrates how to use modern AI agent "harnesses" to automate the generation, compilation, and debugging of Arduino and Python code.

**Key Concepts:**
*   **Agent Harnesses:** The instructor introduces this term to describe software (like **OpenCode** or **Claude Code**) that sits atop an LLM (like Gemini or Claude) to orchestrate complex tasks, such as interacting with a computer's shell, managing file systems, and running external CLI tools.
*   **Vibe Coding:** Using AI to write and iterate on code through natural language prompts. The instructor emphasizes that while AI can "do everything," it is often better to work in smaller, testable increments.
*   **Arduino CLI:** A command-line interface for Arduino that allows AI agents to compile and upload code to hardware without a traditional GUI.

**Technical Demonstrations:**
1.  **OpenCode & Gemini Pro:** The instructor uses OpenCode to identify hardware ports (COM3) and generate a "Hello World" Arduino program for the M5StickC Plus 2.
2.  **AI Debugging:** When the AI’s serial monitor fails, it autonomously suggests and writes a **Python (PySerial)** script to read the serial output from the device.
3.  **Complex Graphics:** The instructor prompts the AI to create a program displaying a **spinning purple square** that moves up and down. The AI handles the coordinate math and rotation logic using the `M5Unified` canvas functions.

**Hardware Theory (M5StickC Plus 2):**
*   **M5Unified Library:** A cross-platform library that allows a single piece of code to run on various M5Stack devices (StickC, Core, etc.) by handling board-specific pin assignments internally.
*   **Code Structure:**
    *   `M5.config()` & `M5.begin()`: Essential for initializing board-specific features like display backlight and speakers.
    *   `M5.update()`: A critical function for **polling hardware state**. It handles button debouncing and event detection by tracking state changes between loop iterations.
    *   `M5.delay()`: A "smarter" version of the standard delay that optimizes power consumption.
*   **Efficiency:** A discussion on the importance of bits (e.g., the value 255 in 8-bit systems) and why code optimization remains vital in the resource-constrained world of microcontrollers.

**Class Administration:**
*   The class concluded with a **multiple-choice quiz**. 
*   The instructor hinted that future quizzes might be **personalized**, based on the specific code each student generates using AI tools, emphasizing the need for students to understand and explain AI-generated outputs.