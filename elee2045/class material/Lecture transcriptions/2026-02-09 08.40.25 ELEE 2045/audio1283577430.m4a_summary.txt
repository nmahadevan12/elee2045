This audio file is a recording of a university-level lecture focused on developing applications for the M5Stick (an ESP32-based microcontroller) using the Arduino framework and Python for serial communication. The instructor covers hardware safety, efficient graphics rendering, and the nuances of data transfer between devices.

### **Transcription**

[00:00:00] **Instructor:** Alright everyone, hope you had a great weekend. Hope you made some money on the Super Bowl; I did, I actually won what I bet. Anyway, I’m super happy about it. So, the topic for today is just going to continue on with how these things work, what are the “gotchas,” preparing you for assignment two, which you probably have read through at this point. 

[00:00:35] It’s weird that COM3 doesn't exist, I don't know if I actually clicked it or whatever. But anyway, this is running a sort of program right now where it's toggling through the different images in a folder on the computer that are being sent. If you hit various buttons, it does stuff, right? Turns the screen on or off. This will request the next image. 

[00:01:05] It is possible, so I did this in a few different ways, ranging from using AI tools, coding myself, to just kind of like code assistant type work. All of them kind of work. Depending on if you use the best AI that's out there, it's going to go a lot faster. If you don't use the best AI, it's going to go a lot slower. But the research on all of this is actually quite clear: the more you do yourself, the more of the code that you actually remember. 

[00:01:45] And so, I am going for the next quiz, which will be on Monday next week, I am going to quiz you on your actual submission. I’m going to do it. And so, you need to know your code. You need to take responsibility for the code that you write, whether the AI wrote your code or you wrote your code; it is still code that you own and you are responsible for. 

[00:02:20] The exception to this will be if you do not submit something for assignment three, or your submission is so bad that I can't quiz you on it, you will be quizzed on a solution that I provide to you. Okay? And so, you have to have that fallback. It's kind of like the NFL's kicking rule, right? You have to tell the other team when you're doing an onside kick. Very similar. Alright, so there is a fallback, but you're going to have to take ownership over the code that you write. 

[00:03:00] So, I'm trying to compile this right now. This is actually the next thing in the lecture that we started to talk about: what does this thing actually do? We talked about this program basically in its total, or at least got close to it. I think we actually got through a little bit, but not all of it. But the basic idea of this program, to reiterate a little bit, you've got to read through it pretty quickly to get a sense of it. 

[00:03:35] Basically, if you press the button, it's supposed to print "Hello World" and play a little tone. If you hold down the button, it's supposed to do something else. So you can do these various things, and all these happen as a result of—let me make this a little easier—all of these functions like `buttonA_wasPressed` or `buttonA_wasHeld`. These things happen as a result of doing this `M5.begin` and the `M5.update`. 

[00:04:15] If you don't write `M5.update`, these functions won't work. The `M5.wasPressed` and the `M5.wasHeld` functions won't work; they won't function at all. The reason they won't function at all is that the `M5.update` keeps track of the last state of the button and updates these other values to check whether or not they were pressed or if they were held. And so, it's got to be able to keep track of that state. We discussed this right at the end last time: if the button is down currently, you don't want to keep firing these events. 

[00:05:00] And so, these will only happen when certain conditions actually occur. So these are what's called event functions. This is one way of processing events. The other way of processing events would be to like hand off a function to be called by the event tracking system when those things happen. That would be more of an event-driven system. This is more of a procedural system that happens to be able to detect events—a little bit different of a paradigm, but ultimately doing a very similar thing. 

[00:05:40] You can see a bunch of other things on here. I pointed you to some more information about the M5 Unified Library or package where all of this stuff actually comes from. And so you have things like setting the display rotation, setting the speaker volume... the volume's actually also proportional to the frequency. I think it's like 2,000 or so is the center, it might be 4,000. I probably use 4,000 here for a reason. But if you use 4,000, you should be able to hear it. 

[00:06:20] But yeah, it's going to go through, it's going to play a tone. This value on here is the tone duration in milliseconds. When you play these tones, these functions should end immediately. And so when you say play a tone for 100 milliseconds, you're actually handing that job off to the tone generation system, and it plays it for 100 milliseconds and your code kind of continues on from here. It doesn't just pause for 100 milliseconds while it plays the tone. That will vary, actually, depending on the function, whether or not there's going to be a pause. 

[00:07:05] So, for example, like a delay function is almost always synchronous because you want the program to literally delay. There is no like wait function in a processor typically. Processors are always on, they’re always doing something. But they can actually go to sleep for that time. They can do something, but there's some amount of wiggle room in some of that. Some delay functions don't cause the processor to sleep and they are a deterministic amount of cycles of the processor before they come back to you. Others do go to sleep and they rely on an internal clock to wake them up. 

[00:07:55] Putting these boards to sleep, by the way, is a good idea if you're trying to conserve battery life. Even putting them to sleep for a second will save a significant amount of battery life. That also typically means when you put it to sleep that it can't do other things, so you have to be little careful with that. The display obviously would continue running. 

[00:08:20] So anyway, there's a little delay on there. This will just cause it—like having these delays in here can be somewhat important to make sure that certain things process in the background. The M5 does have the possibility of running multiple tasks, and so using the `M5.delay` is probably a good way of ensuring that another task, if it's waiting to run in the background on the current core that you're on, that it allows it to run. 

[00:09:05] So again, if checked... so this loop runs repetitively. So you don't see a loop within this loop function because `loop` is being called repetitively. And it checks if the button was pressed and it then does this print line. We talked about the print statement a little bit last time, but this display is a little bit different. So `print` is printing to the serial port. `M5.Lcd.print` prints to the actual M5 display. 

[00:09:45] And so, because we set rotation 1, it should be horizontal. If you set rotation 0, it would actually be vertical. And so there's actually a little print routine in here that lights up individual pixels on the display. It's being hidden from you, but if you actually boil down, you'd actually see LCD commands where the LCD that's hooked up to this has very specific ways that you turn on and turn off pixels. And so there's a routine here for `println` that makes the right pixel show up for "Hello World." 

[00:10:30] And you can set font sizes and things like that. In the same vein, there's a speaker and you can set a tone for a certain amount of time. If I hold the button, though, it's going to do something a little different. You see this `setCursor` here? `setCursor` is what you imagine: there's essentially where it is currently drawing. And so this is usually related to text. Setting cursor to a certain place is where it's going to start drawing text. And so you have to set cursor before you start drawing text. 

[00:11:15] This `clear` is actually clearing out the display. And so when you hold the button A, it's going to clear the display, play a tone again, and then it's going to draw "Clear." So the purpose of this program is to illustrate how the display does printing, because every time I hit the button it's going to print "Hello World," but we'll see it'll just continue to tack on "Hello World." 

[00:12:00] Let's actually build on that. By the way, if you copy this program from the PowerPoint, it's going to have all these weird special characters. Building in here can take a little time, but it's just using the Arduino CLI underneath the surface. I already compiled it once, though, so it should go fast. Compiling a program is essentially taking these individual C++ files, of which there are many. You're seeing the tip of the iceberg, but the reality is there's a whole bunch of libraries, individual C++ files, all of those have to be compiled and they're turned into these binary blobs and then they're all linked together. 

[00:13:10] Alright, so right now we have a blank display, but if I hit it, it's very hard for you to see because it's very small, but it does say "Hello World." I do it again, it does "Hello World," "Hello World," "Hello World"... and it's just continuing to add to the display. And if I hold it down, it says "Clear." And then it goes away because I just delayed a thousand after the clear and then I cleared it again. 

[00:13:50] Just some "gotchas:" if you don't call `M5.update`, these won't work. You can do it yourself if you want to. You can check the states of the buttons yourself. You can actually use ordinary Arduino functions like `digitalRead` and `digitalWrite` if you want to write an output pin. The thing is, though, if you use those, you need to know what pins you're actually driving. You can probably also look up for the M5 what's the actual pin map, but you need to make sure you know what pins are actually being driven. 

[00:14:45] Writing is usually the problem; reading is almost never a problem. But if you're writing a pin, you need to make sure that what it's connected to you actually want to set to high or low voltage. The side effect, by the way, if you happen to do that and happen to mess something else up, you probably blow a digital pin and that's about the end of it. The digital pins on this, by the way, are not capable of outputting a decent amount of current; they are very limited, typically about 40 milliamps or so before they'll break. 

[00:15:15] The biggest danger of these things is the lithium polymer battery that's inside them. If that gets damaged, that can catch on fire. The actual voltage from the battery is not a problem, but the fire could definitely be painful. So be a little careful with it. If this thing gets crushed or damaged in some way, don't put it in your backpack. You'll notice if you notice this thing gets a swelling, take it out of your backpack and put it aside. 

[00:16:00] Generally speaking, they don't catch fire, but they start giving off toxic gas. You all have phones in your pocket that have the exact same batteries in them. Same deal. If you notice your phone start to swell, take it out of your pocket. There's a reason they don't let you keep batteries in suitcases on airplanes. 

[00:16:25] So, there is a problem, though, actually with this program. This one's not a big deal because it's writing and it's just stopping, it's not writing over and over and over again. What you'd notice if you did try to build over and over again... if you build this, what you'll see is that the display will flicker. And so that flickering comes from the fact—from actually one function in here that's being repetitively called: `M5.Lcd.clear`. 

[00:17:10] So this `M5.Lcd.clear` takes a decent amount of time to run. And it runs somewhat asynchronously. And the other problem is this `drawString` here also doesn't sort of wait or anything like that; it literally draws the pixels as the display is also drawing. And so most display systems use what's known as a write buffer basically that you can write to and also do other operations to. 

[00:18:00] And so the clear is happening and then the clear is going to take some time to run, and then the display is going to run and they're sort of fighting each other and the end result of all this is a flickering effect that happens on the display. And so there's a much smarter way to go about drawing graphics on the M5 stick, and that's using **Sprites**. 

[00:18:35] And so this is how almost all games work and how your computer's working right now, is that the drawing is happening to a memory buffer that is not being currently displayed. And so you have a buffer that is being displayed, a block of memory that's actually being shown on the screen, and then the actual graphics are being drawn over to this other buffer. In M5 lingo, this is Sprites. 

[00:19:15] The idea of the Sprite is that you can essentially say "I want a Sprite of the same size as the display." The actual object is called the `M5Canvas`. So the result of this is a canvas. So you get this Sprite and it's the exact size of the M5 display. By the way, the individual pixels on the display are 16-bit. You'll learn this as you're going through the photo frame, but it's 5 bits for red, 6 bits for green, and 5 bits for blue. You get a little extra resolution in green because our eyes see more green than they see red or blue. 

[00:20:10] So you see the `sprite.setTextSize` and `Lcd.setTextSize`... both of these are effectively graphics objects that have the same functions on them. In C++ lingo, they both inherit from the same parent class. One's a block of memory, the other one's the actual display that you're writing to in real time. 

[00:20:45] But now you see these Sprite functions. `fillSprite(BLACK)` will make it an all-black Sprite. And then we can do `drawString`. And then a big difference here is it does `sprite.pushSprite(0, 0)`. And so `sprite.pushSprite(0, 0)` draws the Sprite in its entirety to the display in one shot. Okay? And this will completely eliminate the flickering effect. You won't see that anymore. 

[00:21:28] The downside of this is it effectively limits your program to running at 50 updates a second. I think the internal display LCD refresh rate is 50 times a second; it will wait until the display, it'll draw that and then it'll actually flip it basically at that moment in time. 50 times a second is generally speaking perfectly fine for any program that you're going to write that has an interactive display on the M5. 

[00:22:00] So let's write these out. I'll show you what happens if you, by the way, copy from the PowerPoint. It's a good idea for muscle memory to write these yourself. You're going to get all these extra pieces in here. One strategy you could use, by the way, is take it, copy it into an AI tool and say "give it back to me" and then it doesn't have those problems. I still can't figure out why PowerPoint has this weird issue to where if you copy Arduino code into it, it has special characters in it when you copy it back out. 

[00:23:15] What they are, they're numbers that don't have symbols to them. And so the characters in your computer are numbers. If they are Unicode numbers, they're 8 to 32 bits typically. ASCII characters, they're 8 bits. And so the letters only take up certain number of those, right? We have 256 possible values. And so there are a bunch of other ones that are still characters, they're just non-printable characters. And so these are these non-printable characters that are still in the text file; they are just not able to be rendered as anything and they're not valid C++ symbols. 

[00:24:12] All right, so we can build this and we can see what the flicker looks like and then we'll see it without the flicker as well. Arduino hides the build folder from you, but the build folder actually has all the compiled C++ stuff. So yeah, you can see the flickering text now that's scrolling across the screen here. That looks terrible, right? Even though what we did was just draw the flickering text, this increases where it's drawing from each time. And then it's resetting it kind of like a marquee. 

[00:25:40] And so we replace this with the `M5Canvas`. It's pretty easy to do that. You get `M5Canvas` just by including the M5 Unified. You see these ampersands a lot in C and C++ programs. Ampersand generally speaking, though, the ampersand is always associated with a memory address of some sort. So when you see address of `M5.Lcd`, instead of the value, which is what you normally get when you write out a variable like this, this is the memory address of that value. 

[00:26:45] So depending on what you are—so in this case it would be the memory address of the `M5.Lcd` object typically. You're getting the memory address, not the actual value. That's what those ampersands mean. A pointer and a memory address are synonymous; it points to where this object lies in memory, all the data lies in memory. 

[00:27:35] So we're just fixing this program again to get rid of the flicker that we're seeing. Instead of `M5.Lcd.clear`, we are going to do `sprite.fillSprite(BLACK)`. You may wonder where these things are coming from. Where is "BLACK" coming from? Where is `M5.Lcd` coming from? We'd have to dive into `M5Unified.h` and the includes inside these to find these definitions. 

[00:29:10] All the drawing functions are identical. Remember we had the flickering text, we should now get a nice smooth text running across. These are important things when you're working with systems is that your job is to notice when that's happening and fix it, right? So this would be, you know, a smell of a poorly designed M5 program if you have any sort of flicker inside it. 

[00:29:55] All right, back over to the PowerPoint so we get into the Python programming side. So this is very important for the second part. The first program you have to write is just Arduino, the second program's going to involve serial communication to a Python program. 

[00:30:25] What you need to know about serial communication on Arduino: there's two ways to read data on Arduino. When you typically do a read on a serial port—and really on a read on any serial stream... and this applies actually to Python and C++ and Java and all these different programming languages... is that almost always by default when you go to try to read something from a stream of data that you don't know when that data is going to arrive, the program just sits and waits for data to arrive before it actually gives you any data back. 

[00:31:10] There's a problem with that, especially for microcontrollers, that you don't want to just sit and wait. You're also keeping the lights on, keeping a tone running, or doing a lot of different stuff that needs to happen and so you can't just sit and wait. You can't just sit and do nothing. And so `read` is typically bad to run unless you know something's actually there. 

[00:31:35] There's a buffer. So even if our program is not reading data, if another program or the counterpart wherever it's connected to is sending data to the serial port, the serial port would just fill up the buffer. Sometimes this is known as a FIFO, first-in-first-out. And so it's filling up this queue of data so that when something does read it, it's going to read from that buffer. That buffer has a finite size to it, it can overflow and then you'll start losing data and so you do need to attend to the serial port fast enough to consume that data. 

[00:32:20] And so this `Serial.available` will check and make sure that there's something available before you attempt to read. `Serial.read` will return exactly one byte from the serial port. One 8-bit value will go into this `uint8_t`. That is a type in Arduino C++... unsigned integer, 8 bits in width. 

[00:33:15] You're getting back these 8-bit values from this generic stream of bytes. And the sender, right, has a responsibility here of telling you or documenting what those bytes actually mean. All right, so let's say you write a Python program and you want to send the value, let's say 432. 432 is greater than 256, right? So how do we encode that into a byte? Well, there's a few different options. 

[00:34:10] The normal thing is we're going to use a 16-bit deal essentially or 16 bits to specify an integer. Well, that comes with some problems too. If I'm going to send a 16-bit value over a serial port and the guy on the other side is going to read in 8-bit values... okay, you might say, "Okay, you just read two of them, no problem." You got read two, put them together... but the bigger problem is that you need to know which byte came first. 

[00:34:55] When you are sending something like a 16-bit value or a 32-bit value or whatever number, you need to know the **byte ordering**. The 16 bits... there's a way you write those out on a piece of paper. That is not necessarily the way they are stored in memory. And so sometimes the little end comes first, and sometimes the big end comes first. When the little end comes first, that's what's known as **Little Endian**. 

[00:35:45] And literally computer architectures can be one of two types typically: Little Endian and Big Endian. And that is which way they store multi-byte integers typically and floats can be the same way. Now you might say it makes perfect sense to store the big byte first, the Big Endian. That's actually the way that most ARM processors work nowadays. And the reason for the Little End and why actually a lot of older systems use Little Endian... I believe this actually uses Little Endian, almost all Windows or Intel systems use Little Endian... the little end is actually needed first if you're doing like an addition or something like that. 

[00:36:35] You don't start at the other side when you're doing a long addition, right? You start at the little side first and then you go the other way. And so Little End does make sense from a performance standpoint, but it's actually confusing when you are just like reading it. And so if you're looking at the data, right, you're reading it typically 0-1... you have to just know that the little end actually came first. 

[00:37:15] And so it is important to know when you're doing any sort of communication over a single wire that can only send one bit at a time, you need to know if it's sending Big Endian or Little Endian and you get to make that choice when you're designing both sides. If you're not designing both sides, meaning something else is sending this to you, you need to then convert it. It's always possible converting it, you just need to know which side came first. 

[00:37:50] So that's a very important consideration when you're designing serial because you're going to be getting single bytes. You can also read multiple bytes, you can read into a buffer, and that's actually the way that I typically recommend that you read multi-byte values is you read them in in a block and you say, "If you're going to send something to me, send it in a way I can read it." Put it in Little Endian for me so I can read it. And then I can read it into a buffer that is two bytes wide and then I can convert that buffer directly into an integer, done. 

[00:38:40] So two things you need to know about serial: one is you should always check if there's something available so you can attend to things like button presses and stuff like that instead of while you're waiting for stuff to come in on the serial port. The other one is know that you're reading individual bytes. That's about it. There's also serial writing. Serial writing's a lot simpler because you're just writing, you're just sending it out. 

[00:39:15] I guess there's one additional thing with the writing is that you can write so fast that you're exceeding the capability of your serial port to actually write those bytes. But these writes become synchronous once the serial buffer gets full, so basically your program will just slow down at that point. Normally you're just putting data into the buffer, but if the buffer is full, it'll just sit there and your program will sit there while it's waiting to write. That's usually not the problem, though, usually the problem is the reading. 

[00:40:00] All right, so that's serial on Arduino. This is what serial looks like in Python. There's a few more imports that you need. AI will write all these for you, but there's of course this `pyserial` that you need to import. You also typically need a function to list ports or it's useful to list the ports if you don't know the names of them. And then oftentimes you will import `time` in Python programs that you write just so you can do like sleeping and telling them to wait and stuff like that. 

[00:40:40] This is pretty important in the case of serial communication to a device like this because if you write too fast for this thing to read, it's going to be a problem. Of course your computer is much faster typically than the Arduino is and so you can overwhelm this with data and so typically you are going to sleep. We've already mentioned the baud rate, so serial of course has a baud rate that is bits per second. Technically it's symbols per second, but it's bits per second. 

[00:41:25] 115200 is a fairly good value to use and so you see that corresponding value over here. They have no way of communicating that value, so they have to be set to the same value in both programs. All right, so you've got your serial list ports and the reason I did this this way is that if you don't do this, you just have to know what that value is. But in Python you can query and see what values that are actually available to you. 

[00:42:05] If you are actively using the serial monitor, so if I have this open, that has the COM port open, your Python program cannot open the COM port too. Only one program can have the COM port open. That includes when you're compiling and stuff like that and so if you have your Python program that has it open and you go to try to build an upload, that is also not going to work because your Python program has it open. 

[00:42:45] I get these questions a lot. I will get them again about what's not going on or why things aren't working—it's probably that you have your serial monitor open. The Arduino IDE can sometimes, even if the serial monitor seems to be closed, it can still have the COM port open and so every once in a while you're going to have to close the Arduino sketch to actually kill the or release its lock on the serial port, unfortunately. 

[00:43:30] So again, very important part that you can only have one device have the serial port open. So it's going to use `ports[0].device`... `serial.Serial` will work with that device name. You wouldn't have to go through all of this if you just wanted to know, "Hey, it's COM3," just put "COM3" here and move on. And we're opening it at 115200. 

[00:44:05] And then it goes in a `while True` loop, so forever, and it says a very similar thing: `while M5Stick.in_waiting > 0`. And so `M5Stick` being the name of the serial port object, it checks if there's anything there. It's doing the exact same thing the Arduino program is doing: it's checking if anything is there. If something is there, it will print that. If nothing is there, it's going to sleep for 0.1 seconds. In Python your waits are in seconds, when you delay in Arduino those times are in milliseconds, not seconds—just something to be aware of. 

[00:44:55] And then it writes out "Hello World." The 'b' at the beginning means bytes. So this "Hello World" here is not a string, so the "Hello World" here is not a string; it is bytes that happen to be given to this write function as a string. It is the bytes of this string. You can actually encode individual like byte values with these byte strings as well, but it's a string encoding. You've seen like f-strings and r-strings in Python... these are b-strings. They are bytes. 

[00:45:45] The other way that you write this is arbitrary bytes, so you can write a bytes object. This `bytes([1, 2, 3])` would write the bytes 1, 2, and 3—the integer representation of 1, 2, and 3. And some of this has to be known, by the way. There are other ways to encode stuff. You don't have to follow that convention. You can encode things however you want to encode them, but you don't get support from Python from doing it. 

[00:46:25] So you want to flip everything around and encode your values in a weird way, you're perfectly welcome to, but it will encode these as in the typical way, you know, sort of from left to right as you would expect in binary. This will write out "Hello World," this will also write out this random set of bytes. 

[00:47:00] Long story short on all of this is that writing individual bytes, writing individual strings and things like that is actually not the smart way of communicating with microcontrollers. You can certainly do that. And it's useful to write through programs and you can see like this does a write on here. Structured data is generally the way that I recommend that you read and write things. 

[00:47:45] And so if you define something over in here... so I want to send some structured data to the M5 stick, I can define a structured format. This says I'm going to use a float, by the way this is going to be all Little Endian, 4-byte integers. So this is saying a float and an int. There is an actual specification of this in Python, so you can say, "This is how I'm going to write this structured data out, it's going to consist of a float, it's going to consist of an int, and it's going to be Little Endian." 

[00:48:30] So I give the `struct` package how I want to organize this structure and then we can actually pack our data into that structure and then send it. And so that's the right way to do this communication: tell Python this is how to structure the data, tell Python to pack this data in that way, and then you send this byte structure over to the other side and then you read that structure into literally an identically typed structure: `float`, `int`. 

[00:49:15] So remember put the burden on Python to do all that structuring and we just read it straight in and you can see it actually read in right here. This does a write and it actually literally takes the packet, it takes the pointer to the packet and you'll automatically just get that, so it'll send the bytes that's in that block of memory over to the other side. 

[00:49:45] So it's 9:30 now. You actually have plenty to get started with this. You have everything you need to be able to send over structured blobs that represent images from the Python program. There's not a whole lot else there besides reading those blocks of data from images. You're using a library for that anyway. So I will see you all on Wednesday. Make sure you're getting started on your programs, they are due Wednesday night. Alright, see you then.